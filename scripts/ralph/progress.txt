# Ralph Progress Log
Started: Thu Jul 23 11:26:53 CEST 2026
---

## Codebase Patterns
- **Check the working tree and `origin/main` before starting.** This batch began with local `main`
  one commit behind `origin/main` (US-01 had already been squash-merged as #26) and a stale
  unmerged `prd.json` in the index. Branch from `origin/main`, not from local `main`.
- **Parsers throw, they do not return errors.** `parseGroup`, `parseWeekColour` and now
  `parseCardNumber` all raise a typed `DomainError`. Where a screen has to render whether or not the
  rule succeeds, add a *total* form beside the throwing one (`findLowestFreeNumber` /
  `lowestFreeNumber`) and write the throwing one in terms of it — two entry points, one statement of
  the rule. Do not add the total form speculatively; wait for the caller.
- **Domain modules are named for what they do, not for their file**: `formatCardNumber`, not
  `format`. A PRD that writes `format(...)` is naming the concept, not prescribing the export.
- **Derived, never stored**: card numbers, household counts, portion allowance, price. If a value can
  be computed from the birthdates plus the settings in force, it has no column.
- **100% branch coverage on domain + application is a hard gate.** Every guard you add needs a named
  test, so do not add a defensive branch you cannot justify with a rule.
- `eslint`'s `react-hooks/error-boundaries` forbids constructing JSX inside a `try`. Await into a
  variable inside the `try`, build the JSX after it. `npm run build` does not catch this; lint does.
- Next 16 hands a dynamic route its `params` as a **`Promise`** — `await` it, and treat a
  non-numeric id exactly like an unknown one.
- `germanDate()` is duplicated in `einstellungen/page.tsx` and `kunden/[id]/page.tsx`. **Worth
  extracting to a shared UI date formatter in the next story that touches either page** — US-004's
  card view is a candidate.
- Playwright specs share `data/e2e.db` and the customer-number sequence. Assert against the number
  the screen *proposes*, never a hard-coded id, or inserting a spec above breaks the run.
- **A port method takes a value object, not loose arguments, whenever a field has to come from the
  clock.** `cards.issue(customerId, { index, issuedAt, reason })` keeps the stamping in the
  application layer; an `issue(customerId, index, reason)` would have left infrastructure to invent
  the time and broken the one-wall-clock rule.
- **Every use case reads `deps.clock.now()` exactly once**, into a `const now`, and uses it for both
  the record and the audit entry. Two reads would make the same event carry two instants.
- **Fakes live in `src/application/customers/customers.test.ts`**, one class per port, and each
  imitates the *database's* behaviour rather than the happy path — `FakeCustomerRepository.stealNext`
  fakes a lost race, `FakeCardRepository.place` leaves a gap in the card indices. Write the fake so
  the rule can actually fail.
- `prisma migrate dev` refuses to run non-interactively when the dev database is ahead of the
  migrations folder. Delete `data/fd.db` first (`npm run db:reset` does it), then `--create-only`,
  hand-edit the SQL, then `prisma migrate deploy` + `npm run db:seed`.

---

## [2026-07-23 11:30] - US-001 (CardNumber value object)
- Grew `src/domain/card/cardNumber.ts` from the format-only helper US-01 left behind into the full
  value object: a `CardNumber` interface (`{ customerNumber, index }`), `parseCardNumber(text)` and
  `nextCardNumber(card)` beside the existing `formatCardNumber(customerNumber, index)`.
- Added the `InvalidCardNumber` error class to `src/domain/errors.ts`. Its `code` was already in the
  `DomainErrorCode` union, reserved by an earlier story — the class was the missing half. It carries
  the text as entered, like `InvalidEuroAmount`.
- Parsing rules, all named tests: accepts an uppercase `K` and surrounding whitespace (someone
  copying a number off a card at the counter produces both, and neither names a different card);
  rejects `''`, `'50'`, `'k3'`, `'50k'`, `'50k0'`, `'0k1'`, `'050k3'`, `'50k03'`, `'50k3x'`,
  `'-50k3'`, `'50k-3'`. Output is always the lowercase `k`, documented on the pattern constant.
- Files changed: `src/domain/card/cardNumber.ts`, `src/domain/card/cardNumber.test.ts`,
  `src/domain/errors.ts`, `docs/technical_documentation.md`, `scripts/ralph/prd.json`,
  `scripts/ralph/progress.txt`.
- Documentation: `docs/technical_documentation.md` gained a `src/domain/card/cardNumber.ts` section
  (derived-not-stored, the three functions, why the leading zero is rejected, and FR-6 — card numbers
  are *not* unique across the archive because slot 50 is reassignable). The file tree and the
  `errors.ts` class list were updated; the list had also been missing `CustomerNotFound` and
  `InvalidCustomerRecord` since US-01.
- All gates green: lint, typecheck, coverage (domain + application 100% on all four metrics, 243
  tests, up from 224), build.
- **Learnings for future iterations:**
  - **Deviated from the PRD's export names on purpose**, recorded in the story's `notes`: the PRD
    writes `format` / `parse` / `next`, but the file already exported `formatCardNumber` and the
    domain's convention is `parseGroup` / `parseWeekColour`. Read a PRD signature as naming the
    concept, not dictating the identifier; record the deviation rather than silently renaming
    working code.
  - The PRD said `parse` "returns a typed `InvalidCardNumber` error". It **throws** it, because every
    other parser in this domain throws and a lone returning one would make callers branch two ways.
    US-04 (lookup by card number at the counter) is the story that may genuinely want a total
    `tryParseCardNumber` — add it *then*, in terms of this one, not speculatively.
  - The regex is built from the `CARD_INDEX_MARKER` constant with the `i` flag, so the marker is
    stated once. `[1-9][0-9]*` on both parts is what rejects the leading zero and the zero itself in
    one expression — no extra branch, so no extra coverage burden.
  - `next` returns a fresh object; a test asserts the input is untouched. Domain value objects here
    are `readonly` interfaces, never mutated in place.
  - **US-002 next**: the `issueCard` use case. `nextCardNumber` deliberately does *not* decide that a
    card is due — the application layer reads the highest issued index (US-003's `currentCard`) and
    calls it. Validity stays derived from being the highest index; do not add a `valid` column.
---

## [2026-07-23 11:40] - US-002 (issueCard use case)
- Added `src/application/customers/issue-card.ts`: the single path by which any card comes into
  existence. Reads the clock once, loads the customer (`CustomerNotFound`), refuses an archived one
  (`CustomerArchived`, new error) but serves a blocked one, takes `currentCard(customerId)` + 1 via
  `nextCardNumber` (1 when they hold none), writes the card and the audit entry under
  `customer.card.issued` with the reason as the `why`.
- Added the type-only `src/domain/card/card.ts`: `IssuedCard { index, issuedAt, reason }` and
  `CardIssueReason = FIRST_ISSUE | LOST | STALE_COUNTS | OTHER`. No `valid` flag — validity is being
  the highest index (FR-4).
- Added the `CardRepository` port (`currentCard`, `issue`) and the `CustomerArchived` domain error.
- 12 named tests against a new `FakeCardRepository`: first issue, second issue, a gap in the run
  (1 and 4 → 5), earlier cards kept, clock stamping, the reason on the card, the audit entry,
  blocked served, archived refused, nothing written when refused, unknown id refused.
- Files changed: `src/domain/card/card.ts` (new), `src/domain/errors.ts`,
  `src/application/ports.ts`, `src/application/customers/issue-card.ts` (new),
  `src/application/customers/customers.test.ts`, `docs/technical_documentation.md`,
  `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`.
- Documentation: `docs/technical_documentation.md` gained `src/domain/card/card.ts` and
  `src/application/customers/issueCard` sections; the ports table, the errors list and the file tree
  were updated.
- All gates green: lint, typecheck, coverage (domain + application 100%, 254 tests, up from 243),
  build. No UI changed, so no browser verification was needed.
- **Learnings for future iterations:**
  - **Deviated from the PRD's port signature on purpose** (recorded in the story's `notes`): the PRD
    writes `issue(customerId, index, reason)`, but `issuedAt` has to come from the injected clock, and
    that signature would have left the adapter to read the wall clock. The port takes the whole
    `IssuedCard` instead.
  - `registerCustomer` still writes the first card inside `customers.create(…)` rather than calling
    `issueCard`: US-01.4 requires the customer and their first card in **one transaction**, which a
    second use-case call cannot be part of. "One card-issuing path" means one *decision* about the
    next index, not one SQL statement. Do not refactor this without a transactional port.
  - The `reason` column does not exist yet, so `CustomerCard` in `customer.ts` is still
    `{ index, issuedAt }` while `IssuedCard` carries the reason. **US-003 should unify them** when it
    adds the column — that is the story that owns the schema change, and adding the column here would
    have been a half-migration.
  - A type-only domain module shows as `0 | 0 | 0 | 0` in the coverage table (like `ports.ts`) and
    still passes the 100% gate — v8 reports no statements rather than uncovered ones.
  - **US-003 next**: `PrismaCardRepository`. It needs the `reason` column, the confirmed
    `@@unique([customerId, index])`, and a typed error for the lost race — the port's doc comment
    already says the adapter is the final authority on a free index.
---
