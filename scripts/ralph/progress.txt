# Ralph Progress Log
Started: Fri Aug 28 2026 (batch 29 — US-29 the customer balance)

The `## Codebase Patterns` block below is CARRIED OVER from earlier batches on purpose
(`scripts/ralph/prds/README.md` point 2). Add to it; do not reset it.

## Codebase Patterns
- **Adding a `DomainErrorCode` is a three-file change**: the union in `src/domain/errors.ts`, the
  class beside it, and `TIERS` in `src/app/notice-tier.ts`. `TIERS` is a `Record` over the union, so
  the *build* fails until the code is tiered — that is the mechanism, not an obstacle. The header
  comment of `notice-tier.ts` counts the codes ("a 32nd code…", "the six red ones"), so it goes
  stale silently; update it with the entry.
- **The same mechanism guards `SettingsChange`**: its `switch` in `src/app/einstellungen/page.tsx`
  is exhaustive with no `default`, so adding a member to the union fails the build until the screen
  renders it. Do not silence it with a `default` branch.
- **An error class with no caller yet still needs a test**, because `domain/` coverage is gated at
  100%. `src/domain/errors.test.ts` is where those live — see the `MissingAuditReason` block for the
  shape, including the comment saying which story will call it.
- **`errors.ts` cannot import from `domain/card/`**: `cardNumber.ts` imports `errors.ts`, so a
  message that wants to read `66k1` writes the `k` inline rather than calling `formatCardNumber`.
- **Adding a field to `Settings` is a wide change before it is a deep one.** `createSettings` is
  called from the repository, the seed, the settings server action and every hand-written fake in
  the application tests; all of them must supply the new field before anything compiles. Land the
  field with a placeholder value at each call site, then replace them in the story that owns each.
  Get the *files* out of a wide break rather than the errors:
  `npm run typecheck 2>&1 | grep -E "^src/" | cut -d'(' -f1 | sort -u`.
- **A test that merely *uses* a field is not a test *of* it.** Several `settings.test.ts` cases
  reach for an arbitrary second field to prove the same-instant tie rule or the audit's changed-field
  list. When that field goes away, **re-point them**, do not delete them — deleting takes an
  unrelated rule with it and drops coverage below the gate.
- **A Prisma `P2002` cannot always name *which* constraint was broken.** `meta` is
  `{ modelName, target: [columns] }`, but where two unique indexes both cover the offending row
  SQLite reports only one of them, and not the one that was declared or created first. Where the
  distinction matters, match the target for the *shape* and then ask the database the question that
  actually distinguishes the two faults (`card-repository.ts`'s `issue` counts the customer's own
  rows on that index). For a **nested** write Prisma reports the *rooted* model — a card refused
  inside `customer.create` comes back as `modelName: "Customer"` — so the model name alone tells
  nothing about which table failed.
- **Regenerating `prisma/migrations/` drops the hand-written partial unique index** on
  `Customer.customerNumber` (`Customer_customerNumber_onRegister_key`) at the end of the migration
  SQL. `src/infrastructure/prisma/schema.test.ts` greps for it and for the `onDelete: Restrict`
  clauses, so a lost line fails CI rather than production — but re-add it deliberately. Three things
  batches 25–27 each had to learn:
  - **Delete `data/fd.db` before `migrate dev`**: with the old migration row still in
    `_prisma_migrations`, Prisma refuses to regenerate and writes nothing.
  - **Verify with `diff`, not by reading the new file**:
    `git show HEAD:prisma/migrations/<old>/migration.sql > /tmp/old.sql && diff /tmp/old.sql <new>`
    states in a few lines what the story is allowed to have changed. Anything else is a Prisma
    version drifting the DDL under you, which no gate would attribute to this commit.
  - **`migrate dev` applies the migration *before* the hand-written index is appended**, so the
    database it leaves does not have the index even though the committed SQL does. `npm run db:reset`
    afterwards is what makes the two agree — check with
    `SELECT sql FROM sqlite_master WHERE name='Customer_customerNumber_onRegister_key'` rather than
    trusting that the reset ran.
  - A regeneration **renames a directory that documents cite by name**. `grep -rn "<old-timestamp>_init"
    docs/ src/ tests/` afterwards; prefer the `prisma/migrations/*_init/migration.sql` glob, which
    survives the next one.
- **Never `pkill -f "next dev"`, and never `pkill -f "node.*next-server"`** — both patterns match the
  Bash tool's own wrapper command line and kill the shell (exit 144, no output). **And `ss` is not
  installed in this sandbox**, so `ss -ltnp | grep :3000 || echo free` reports "free" for every port,
  always: the `||` fires on `command not found`. (Batch 27's per-story notes recommend `ss` in two
  places — they are wrong; that is the bug this bullet exists to stop.) Check
  `ps aux | grep "[n]ext-server"` — the bracket keeps the pattern out of its own match — and
  `kill <pid>` by number.
- **A stale `next start` from an earlier iteration silently serves a mixed build.** The new
  `npm run start` fails with `EADDRINUSE` in the background while the page still renders, from the
  old server reading the *new* chunks off disk — a couple of console 500s are the only symptom.
  Check for a live server (`ps`, above) **before** trusting any browser check.
- **`npm run db:reset` does not reach a server that is already running.** It deletes `data/fd.db`,
  and SQLite keeps serving the running process from the deleted inode, so the screen goes on showing
  the pre-reset data. Restart the server after a reset, or the verification is of the old database.
  `db:seed` fills settings only — customers are not seeded, so a reset empties whatever manual
  clicking built up (the e2e suite makes its own registers).
- **Changing a seeded policy value moves e2e expectations.** `grep -rn ',00 €' tests/e2e/` before
  editing a number in `seed.ts`.
- **The e2e customer-number bands, as they now stand.** Below the quota of 240: counter 201–206 and
  239, allowance 211, serve 221–222, reminders 231, registration 232–234, card numbers 236. Above it:
  block 241, reissue 251, age-13 271, customer-list 281–285, customer-record 291–293, group-progress
  301–303, group-walk 311–315, price cap 321, eggs 331–334. A spec whose number must be **pickable in the
  /kunden/neu dropdown** has to sit below 240 — the pool is `1..quotaN` — and everything else should
  stay above it, clear of the low sequence the allocating specs (registration, card, archive,
  reregistration) consume. **A new spec takes a new band**; do not borrow another spec's numbers.
- **e2e spec files run in alphabetical path order, one worker, one shared database.** So a spec that
  *writes* shared state is only ever seen by the files sorting after it — `npx playwright test --list`
  prints the order, and a **rename moves a spec in that order** (batch 27's `portions.spec.ts` →
  `allowance.spec.ts` moved it from the middle to second). Anything a spec asserts as an **absolute
  count of a shared thing** is therefore a landmine: `settings.spec.ts` counted its version history
  as `3` and `4` and read the seeded version as `nth(2)`. It now takes a baseline with
  `versionCount(page)` and asserts `before + 1`, and reads the oldest version as `.last()` — the same
  claim, stated so that it does not depend on who ran first.
- **Two consecutive saves on the settings screen need a `page.reload()` between them in e2e.** A
  successful save *clears* the form, and the revalidated render that does it can land **after** the
  next `fill`, rewinding the field to the stored value; the second save then appends a version that
  changed nothing and every history index below it shifts. **`selectOption` loses the race too** —
  this bullet used to say it survived, and batch 29 watched WebKit fail the Ausgabetag spec on that
  claim twice in a row. Reload and re-assert the stored value before touching the field again,
  whichever control it is.
- **The customer record renders two different household blocks.** `/kunden/<active>` gives the live
  editor (`household-editor.tsx`), `/kunden/<archived>` gives `HouseholdReadOnly` in `[id]/page.tsx`.
  Both hold the same tiles, so a screen-level change has to be checked on one of each — **seed
  customer 6 is archived**.
- **Counts baked into prose go stale in files no story names, in both directions.** Removing two
  settings made "the ten fields of the settings form", "nine of the ten need no translation",
  "Seven of the nine settings", "printed only five of the eight settings" and a `toHaveLength(8)` all
  wrong at once; *adding* a field or a tile does the same. After any such change,
  `grep -rn "nine\|ten fields\|eight\|six tiles\|four figures" src/` — the suite catches only the
  assertion. The same applies to an **exported type you delete or rename**: `grep -rn "<TypeName>"`
  finds the `{@link}`-shaped references left behind in comments, which no gate sees.
- **Prettier does not reflow comments.** Shortening a sentence inside a `/** */` block leaves a
  ragged short line that `prettier --check` happily accepts. Re-wrap the paragraph by hand.
- **Prettier owns every markdown table in `docs/`, and `npm run lint` does not check it.** `lint` is
  `eslint` alone; markdown formatting is `npm run format:check` (the pre-commit hook runs it). Widen
  one cell and Prettier re-flows every row of that table, so run `npx prettier --write` on the files
  you touched *before* reading the diff — otherwise you are reviewing a re-alignment.
- **Recording an ADR is four steps, and the script does one of them.**
  `python3 .claude/skills/arc42/scripts/arc42.py new-adr "<decision as a sentence>"` picks the
  number, renders the template and appends the chapter 9 index row as `Pending`. By hand afterwards:
  set `Accepted` in the file **and** the row, **re-count** the intro sentence („The thirteen
  decisions …“), and `npx prettier --write` chapter 9 — the appended row is narrower than the table
  and Prettier re-flows every row of it. Finish with `arc42.py check`, which sees structure and dead
  links but never staleness.
- **A use case that already loads a customer's records must not load them twice.** `recordAttendance`
  reads `listForCustomer` for the once-per-day guard and the balance both; `lookupCustomer` reads it
  for `recordForDay`, `countNoShows` and the balance. One `const history = await …` at the top, and
  everything derived from it — a second query is the thing US-04.3 and US-29 both forbid by name.
- **An unreachable `?? fallback` fails the 100% branch gate.** Where a lookup cannot miss (a row that
  came out of `findById` being in that customer's own list), reduce over the list with a ternary
  instead of `find(...) ?? x` — both arms of the ternary are covered by any test with two rows, and
  nothing pretends to handle a case that cannot arise.
- **Two files carry the story range and neither is anybody's story:**
  `docs/architecture/01-introduction-and-goals.md` („one PRD per user story, US-01 to US-nn“) and
  the `tasks/` row of `docs/architecture/README.md` say the same thing, and both go stale on every
  batch that adds a PRD. `grep -rn "US-01 to US" docs/`.
- **Documentation has no gate.** Nothing fails CI when a chapter goes stale, so a change that adds or
  removes a concept finishes with an explicit sweep — `grep -rin <concept> --include="*.md" .` — and
  the answer is not always zero: `docs/architecture/01`'s non-goals and `09`'s „Decisions that were
  reversed“ exist precisely to keep a withdrawn idea named, with its reason. **`docs/architecture/`
  is the tree to update**; `docs/technical_documentation.md`, `tech_stack_architecture_sketch.md` and
  `fd_dev_setup_overview.md` were retired in #102 and only the archived progress logs still name them.
- **`docs/archiv/` is annotated, never rewritten** (root `CLAUDE.md`). A dated `> **Note (YYYY-MM-DD).**`
  blockquote goes after the *block* carrying the claim, not inside the table cell or list item that
  states it, and names which bullets it answers when it follows several. The per-story PRDs in
  `tasks/` are the same kind of record: they say what was asked for, so a superseded one gets a
  banner and the rest are left alone.
- **A controlled field filled straight after a `goto` needs `fillSticky`, not `fill`** — including a
  `<textarea>`. Hydration re-renders it from the prop and the typed text comes back with the stored
  value spliced into it (`notes-field`, batch 29), which reads like a broken assertion rather than a
  race. `tests/e2e/day.ts` explains the window; the rule is the whole of it: plain `fill` only once
  the page has been interacted with, or on a field React does not own.
- **Before touching an e2e spec your story has no business in, `git stash` and run the suite on
  clean `HEAD`.** It is the only thing that separates a regression from this suite's WebKit
  flakiness. Note that a failure **aborts the files behind it** (`6 did not run`), so one flake hides
  whatever else would have failed — and a green run after a fix is worth repeating twice.
- **`playwright-cli` needs a role locator, not a testid, to fill the counter's Nummer input**, and a
  `cmd-a || cmd-b` shell chain hides a silent no-op — the first command exits 0 without filling
  anything. Run the fill on its own and re-snapshot to confirm it landed. Verify against
  `npm run start` (production build), not `next dev`.
- **WebKit's nav-bar click is flaky at roughly one failure in two full-suite runs**
  (`navigation.spec.ts` „von Kunden verwalten aus …“, `waitForURL` timing out after the click). It
  passes alone and on re-run. If it recurs, the click needs the „wait for React to own the control“
  treatment `day.ts`'s `hydrated` gives the fields (#116) — worth watching rather than chasing.
- **Prior runs' patterns are in the archive**, not in this file:
  `scripts/ralph/archive/2026-08-25-us-28-egg-allowance/progress.txt` is the most recent (the first
  list-valued policy value: a validated staircase, a child table on `SettingsVersion`, a repeating
  add/remove form and a row-by-row diff), `archive/2026-08-24-us-27-remove-portion-allowance/`
  before it (a deletion, run outside-in), and
  `archive/2026-08-07-us-25-globally-unique-card-numbers/progress.txt` keeps a long consolidated list
  (playwright-cli usage, e2e customer-number bands, killing the dev server safely, testid greps).
  Read the most recent two before any UI or e2e story.
- **React resets an uncontrolled form once its server action resolves.** So a field cannot survive
  its own submission's answer: after a refusal that has to be re-submitted — the overpayment
  confirmation is the one we have — the field snaps back to its `defaultValue` while the notice
  beside it still names what was typed, and the second submit sends the wrong number. Every gate
  stays green; only driving it in a browser finds it. The fix is both halves together: derive the
  default from the refusal (`refusal?.paidCents ?? theDefault`) **and** `key` the field on it, so a
  fresh input mounts on the refused value whichever order React re-renders and resets in.
- **`whitespace-nowrap` on a `Stat` value is a narrow-screen bug, and the overflow check cannot see
  it.** A phrase („Offen 2,00 €") runs out through the *tile's* padding at 390px while
  `documentElement.scrollWidth - clientWidth` still reads `0` — the page does not overflow, the tile
  does. Pass the nowrap/width-floor pair only where two tiles exist to be diffed (§4); let a phrase
  wrap where they are not, and read the screenshot at 390 rather than the number.
- **A dictionary entry may not read `de` inside its own initialiser** — TypeScript makes the whole
  object implicitly `any` (TS7022) even though it would work at run time. Lift the shared clause to a
  module-level function above the object, which is what `eggRow`/`fromPersons` already are. Note also
  that `de.ts` is no longer import-free: `formatEuros` and `balanceKind` are imported so that money
  and the balance's wording are formatted in one place and no screen can print a leading minus.
- **`ui_styling_guide.md` §11's snippet still says `pkill -f next-server`, and that kills the Bash
  tool** (exit 144, no output) — the bullet above says why. Kill by pid from
  `ps aux | grep "[n]ext-server"`. And **driving the counter by hand needs `FD_FIXED_NOW_FILE` set on
  the server**, not just a seeded register: the demo day is deliberately not a distribution day, so no
  household is clear to serve. Pin a Thursday whose week colour matches the household (anchor
  `2026-W02` = RED, so odd week offsets are BLUE) and restart `npm run start` with it.
- **Renaming a Prisma column is not finished when `tsc` is green.** A `select: { paid: true }` in an
  e2e spec typechecks against the *generated client*, so `npm run typecheck` on `src/` passes while
  the suite would fail at run time — and five specs held one. After any column rename,
  `grep -rn "<oldColumn>" tests/e2e/ prisma/` and run **both** engines, not just Chromium.
- **A scripted fixture in `demo-seed.ts` must be exempted from the counted patterns, not layered on
  top of them.** `UNPAID_EVERY` and `NO_SHOW_EVERY` reach across the whole cast, so a household
  scripted to demonstrate one state ends on whatever the modulus left it with (batch 29's three
  balance households first came out at −12,00 €, −5,00 € and +1,00 €). Skip the pattern for the
  scripted shape while still incrementing its counter, so the rest of the register is unchanged —
  and verify the fixture by querying the seeded database, never by reading the script.

### Where batch 29 starts

Batch 28 (the egg allowance) is **merged into `main`** — PR #121, squashed as `8187ebf`. So
`Allowance` is `{ grownUps, children, priceCents, eggs }`, the counter/record/editor figures row is
four tiles in four tracks with the price in the fourth, `EggAllowanceRow` hangs off `SettingsVersion`,
and `SettingsChange` already has one member without `from`/`to`. Nothing in this batch touches eggs.

**This batch changes what a hand-out *is*** — `paid: boolean` becomes `paidCents: Cents` — and derives
one balance per customer from the hand-out history. Read `tasks/prd-us-29-customer-balance.md`
§Introduction before writing a line of `balance.ts`; its worked five-week table is the specification.

Five hazards, all stated in `prd.json` and worth reading twice:

- **The sum is against the PRICE, not the amount asked for.** `balance = Σ(paidCents − priceCents)`.
  Against the amount to pay the definition never settles: a household paying the full 8,00 € they
  were asked for gives `8,00 − 8,00 = 0` and their 3,00 € debt is charged again next week, and a
  credit never shrinks. This was an ambiguity in DF's own requirement, corrected and confirmed on
  2026-08-28. The `askedCents` the colour mark compares against is a *different* quantity, derived by
  replaying the history. **Do not "simplify" the two into one.**
- **Story 3 leaves a deliberate bridge.** Between story 3 and story 4 the use cases translate the old
  boolean into an amount (`paid ? priceCents : 0`) — exactly today's behaviour, so the tree stays
  green. Story 4 removes it. Do not leave the tree in that state.
- **Fifth regeneration of `prisma/migrations/`.** Same drill as 25–28: delete `data/fd.db` first,
  re-add the hand-written partial unique index, verify with `diff` against `git show HEAD:…`, and
  `npm run db:reset` afterwards. The patterns bullet above has all four traps.
- **`setPaid` becomes `setPayment` on the port**, so every hand-written fake
  `DistributionRecordRepository` must gain it in the same commit — including the one in
  `read-group-roster.test.ts`, which implements the port in full and has nothing to do with payments.
  The suite will not compile otherwise.
- **`tests/e2e/balance.spec.ts` is a new spec and takes a NEW customer-number band** (see the bands
  bullet above — eggs took 331–334, so start at 341). It also needs a **second distribution day**;
  `tests/e2e/day.ts` already does that for the age-13 and reminder specs. Both engines, registers of
  its own.
---

## 2026-08-28 — US-001 The balance arithmetic (domain)
- New pure module `src/domain/distribution/balance.ts`: `PaidRecord`, `balanceOf`, `amountToPay`,
  `balanceKind` / `BalanceKind`. No I/O, no clock, no framework import; imports only `Cents`.
- New `src/domain/distribution/balance.test.ts` — 17 tests, written first and run red (the module did
  not exist) before a line of it was written.
- Files changed: `src/domain/distribution/balance.ts` (new),
  `src/domain/distribution/balance.test.ts` (new), `scripts/ralph/prd.json`, this log.
- Gates: `npm run lint`, `npm run typecheck`, `npm run test:coverage` (52 files, 1183 tests, domain/
  and application/ still 100%), `npm run build` — all green.
- **Learnings for future iterations:**
  - **The named test that pins the formula is `consumes a credit larger than the price by the week it
    pays for`.** It asserts *both* `balanceOf` and `amountToPay` on the week-4 row, because summing
    against the amount asked for instead of the price passes every other test in the file — a
    household that always hands over exactly what it is asked for gives 0 either way. If a later
    change breaks only this test, the change is wrong, not the test.
  - The module's doc comment forward-references `replayPayments`, which **US-002 adds to this same
    file**. Nothing lints or type-checks a `{@link}`, so the reference is not load-bearing, but leave
    it: US-002 makes it resolve.
  - `distributionRecord.ts` shows as `0 | 0 | 0 | 0` in the coverage table and always has — it is
    types only, so there is nothing to execute. It does not fail the 100% gate; do not "fix" it by
    writing a test that constructs an interface.
  - A test helper named with a snake_case local (`so_far`) passes ESLint here but reads wrong beside
    the rest of the suite; the convention in `domain/` tests is a tiny camelCase factory at the top
    of the file (`entry()` in `groupProgress.test.ts`, `handOut()` here) rather than object literals
    repeated per case.
  - **Docs are deliberately untouched.** `docs/architecture/05` lists `distribution/` module by
    module and would take a `balance.ts` line, but US-010 owns every document in this batch — adding
    it early would make that story's diff lie about what it did.
---

## 2026-08-28 — US-002 The payment replay and the overpayment guard (domain)
- `src/domain/distribution/balance.ts` gains `PaymentStanding`, `standingOf`, `Settlement<T>` and
  `replayPayments` — the same module as US-001, because the replay is the balance walked in order.
  `replayPayments` sorts a **copy** by `date` ascending itself; `askedCents` per row is
  `amountToPay(priceCents, balance of the earlier rows)`, so it is exactly what the counter showed
  that day. No `settlementOf` lookup helper, on purpose (a second way to ask the same question).
- `src/domain/errors.ts` gains `OverpaymentNotConfirmed(paidCents, amountToPayCents)` and its code;
  `src/app/notice-tier.ts` tiers it `refusal` (a question, not a fault) — its stale header count went
  from „a 36th code" to „a 37th code" with it. No caller until US-004.
- Files changed: `src/domain/distribution/balance.ts`, `src/domain/distribution/balance.test.ts`,
  `src/domain/errors.ts`, `src/domain/errors.test.ts`, `src/app/notice-tier.ts`,
  `scripts/ralph/prd.json`, this log.
- Gates: `npm run lint`, `npm run typecheck`, `npm run test:coverage` (52 files, 1195 tests, domain/
  and application/ still 100%), `npm run build`, `npx prettier --check` — all green.
- **Learnings for future iterations:**
  - **The test that ties the two functions together is `agrees with the plain sum`**
    (`replayPayments(records).at(-1)?.balanceAfter === balanceOf(records)`), and it is deliberately
    fed an out-of-order history — in date order it would also pass if the replay never sorted. If a
    later change makes only this one red, the change has split one number into two answers.
  - **`standingOf` compares against `askedCents`, never `priceCents`**, and the test that guards it is
    `marks clearing an old debt as exact, not over`. Every other standing test passes either way,
    because they are all weeks where the price *is* the amount asked for.
  - The forward reference US-001 left (`{@link replayPayments} in US-29.2`) is now resolved and the
    „in US-29.2" dropped — a `{@link}` is checked by nothing, so a story that fulfils one has to go
    and close it by hand.
  - **US-003 renames nothing in this module.** `PaidRecord` is intentionally *not*
    `DistributionRecord`, so the schema change lands beside it rather than through it; the only
    coupling is that `replayPayments` needs its rows to carry a `date`, which the record already has.
  - Docs still untouched — US-010 owns every document in this batch (same reasoning as US-001).
---

## 2026-08-28 — US-003 A hand-out records an amount, not a flag (domain type, schema, infrastructure)
- `NewDistributionRecord.paid: boolean` is now `readonly paidCents: Cents`; the flag is **gone**, not
  kept alongside. `prisma/schema.prisma` follows (`paid Boolean` → `paidCents Int`, with the `///`
  comment saying the balance is derived from it and `priceCents` and never stored), and the
  `SettingsVersion` doc comment lost the sentence claiming a record stores no amount.
- `prisma/migrations/` regenerated as `20260828131514_init` — fifth regeneration, same drill: delete
  `data/fd.db` first, re-append the hand-written `Customer_customerNumber_onRegister_key` partial
  unique index, `npm run db:reset` afterwards. `diff` against `git show HEAD:…/20260825000027_init`
  is **exactly one line**, which is the whole check.
- `setPaid` → `setPayment(recordId, paidCents)` on the port, the Prisma adapter and all five
  hand-written fakes; `RecordRow`/`toRecord` carry `paidCents`. New integration tests: a payment of
  `0` is stored as `0` and not as null (asserted through raw SQL, not only through the adapter), a
  payment above the price round-trips, and a correction down to nothing.
- `prisma/demo-seed.ts` gained a `paymentHabit` on three cast members — `OWES`, `SETTLED_A_DEBT`,
  `IN_CREDIT` — and the closing tally counts part payments and hand-outs paid ahead beside the
  unpaid ones. The scripted households come out at −2,00 €, 0,00 € and +5,00 € exactly.
- The **deliberate bridge** US-004 removes: `recordAttendance` and `correctAttendance` still take the
  boolean and translate it (`paid ? priceCents : 0`), and `lookupCustomer` reads it back
  (`paidCents >= priceCents`). The screens are untouched and the app is green.
- Files changed: `src/domain/distribution/distributionRecord.ts`, `prisma/schema.prisma`,
  `prisma/migrations/20260828131514_init/` (new, replaces `20260825000027_init`),
  `src/application/ports.ts`, `src/application/distribution/{record,correct}-attendance{,.test}.ts`,
  `src/application/distribution/read-group-roster.test.ts`,
  `src/application/customers/{customers,lookup-customer,maintain-customer}.test.ts`,
  `src/application/customers/lookup-customer.ts`, `src/app/kunden/[id]/page.tsx`,
  `src/infrastructure/prisma/distribution-record-repository{,.test}.ts`, `prisma/demo-seed.ts`,
  `tests/e2e/{serve,price-cap,archive,reregistration,customer-record}.spec.ts`,
  `scripts/ralph/prd.json`, this log.
- Gates: `npm run lint`, `npm run typecheck`, `npm run test:coverage` (52 files, 1199 tests, domain/
  and application/ still 100%), `npm run build`, `npx prettier --check .`, `npm run test:e2e`
  (177 passed) and `npm run test:e2e:webkit` (177 passed) — all green. `npm run db:reset` run, and
  the partial index verified in the reset database rather than assumed.
- **Learnings for future iterations:**
  - **A scripted fixture must be exempted from `demo-seed.ts`'s counted patterns, not layered on top
    of them.** `UNPAID_EVERY` writes a zero payment every fifth hand-out across the whole cast, so the
    three balance households first came out at −12,00 €, −5,00 € and +1,00 € — the modulus, not the
    script. They now skip the pattern (`shape.paymentHabit !== undefined || servedSoFar % … !== 0`)
    while still incrementing `servedSoFar`, so which of the *other* households pay nothing is
    unchanged. Any future fixture that demonstrates one state has the same problem.
  - **The bridge is in three places, not two.** `recordAttendance` and `correctAttendance` are the
    obvious ones; `lookupCustomer`'s `TodaysRecordView.paid` is the third, and it is what keeps the
    correction checkbox opening on the stored value. A fourth, `history-paid` in
    `src/app/kunden/[id]/page.tsx`, belongs to US-008. Each carries a comment naming the story that
    removes it — `grep -rn "US-29.3" src/` finds all four.
  - **`correctAttendance` needs the record it is correcting to know the price**, because the bridge
    writes `record.priceCents`. It already loads the record for `canCorrect`, so nothing new is read —
    but a use case that only had the id would have had to.
  - **`select:` lists in e2e specs are a silent break.** Five specs name `paid: true` in a Prisma
    `select`, which typechecks against the *generated client* — so the rename is not caught by
    `tsc` on `src/` alone and would have failed only at run time. `grep -rn "paid" tests/e2e/`
    after any column rename.

## 2026-08-28 — US-004 Recording and correcting a payment (application)
- `RecordAttendanceInput.paid?: boolean` is now `paidCents?: Cents` + `overpaymentConfirmed?: boolean`.
  `recordAttendance` derives the balance from the history **it already loaded** for the once-per-day
  guard (one `listForCustomer`, no second query), then `amountToPay(allowance.priceCents, balance)`.
  An omitted amount **is** that figure; above it without confirmation throws `OverpaymentNotConfirmed`
  and writes nothing; below it — down to 0 — is written silently. Guard order unchanged and now stated
  in the doc comment: eligibility → once-per-day → payment. Audit `changedFields` is
  `["showedUp", "paidCents", "priceCents"]`.
- `CorrectAttendanceInput`'s `SET_PAID` is now `SET_PAYMENT` with `paidCents` and
  `overpaymentConfirmed?`; `REMOVE` untouched. The correction judges the amount against
  **what was asked on that record's own day**, from `replayPayments` over `listForCustomer`, and
  audits `["paidCents"]`. Removal gained no code at all — a named test asserts the balance returns to
  what it was, which is the property derive-don't-store was chosen for.
- The **US-29.3 bridge is gone from both use cases**. The two that remain belong to later stories:
  `lookupCustomer`'s `TodaysRecordView.paid` (US-29.5) and `history-paid` in
  `src/app/kunden/[id]/page.tsx` (US-29.8).
- A **new, smaller bridge in `app/`** takes its place, because US-29.7 still owns the amount field:
  the serve checkbox maps to `paidCents: undefined` (= what was asked) or `0`, and the correction
  checkbox maps to the day's price, carried into the action as a hidden `priceCents` field off
  `counter.lookup.customer.priceCents`. `grep -rn "US-29.4" src/` finds all four sites.
- `prisma/demo-seed.ts` reworked: every hand-out is first written with **no amount** (so the record
  states what was asked), and every deviation — the counted unpaid pattern and the three scripted
  habits alike — is a second write through `setPayment`. Scripts are written against `askedCents`,
  and the tally counts part payments and paid-ahead against it too.
- Files changed: `src/application/distribution/{record,correct}-attendance{,.test}.ts`,
  `src/application/allowance/describe-allowance.ts` (stale „keeps only a paid flag" comment),
  `src/app/ausgabe/{actions.ts,page.tsx,serve-controls.tsx}`, `prisma/demo-seed.ts`,
  `scripts/ralph/prd.json`, this log.
- Gates: `npm run lint`, `npm run typecheck`, `npm run test:coverage` (52 files, 1209 tests, domain/
  and application/ still 100%), `npm run build`, `npx prettier --write` on the touched files,
  `npm run test:e2e` (177 passed) and `npm run test:e2e:webkit` (177 passed) — all green.
  `npm run db:reset && npm run db:demo` run, and the fixture **verified by querying the database**.
- **Learnings for future iterations:**
  - **The balance changes what an unpaid week means in the seed, and it is not a bug.** A household
    that pays nothing is asked for the price *plus the debt* at its next hand-out and — paying the
    default — settles it. So the counted `UNPAID_EVERY` pattern no longer leaves standing debts: only
    the households whose last hand-out was the unpaid one end negative (11 Maier −4,00 €, 15 Kneifel
    −5,00 €). The three scripted ones come out at exactly −2,00 €, 0,00 € and +5,00 € as designed.
  - **A demo script written against the price is wrong now; write it against `askedCents`.** `OWES`
    and `IN_CREDIT` also need every *later* visit pinned to `record.priceCents` — paying the default
    would settle the debt or eat the credit, and the register would have neither state left to show.
    Paying exactly the price is how a household leaves a balance where it was.
  - **The `askedCents` for a past record cannot be looked up, only replayed** — and the replay must
    not fold in the record's own payment. `correctAttendance` reads it off the settlement whose
    `record.id` matches, with a `reduce` rather than a `find`: a `find` needs a `?? fallback` for a
    case that cannot happen, and that unreachable arm fails the 100% branch gate. The reduce's
    ternary is covered by any test with two records.
  - **`counter.lookup.customer.priceCents` already exists** (`counter-lookup.tsx` renders it), so an
    app-layer bridge that needs the day's price does **not** need a new field on `TodaysRecordView` —
    the page hands it to `ServeControls` directly. Worth checking before widening an application view
    for something a sibling component already has.
  - The correction bridge passes `overpaymentConfirmed: true`, deliberately: ticking the box writes
    the day's price, which for a household in credit is more than they were asked for, and a checkbox
    offers no way to answer the confirmation it would otherwise raise.
---

## 2026-08-28 — US-005 The counter reads the amount to pay and the balance (application)
- `CounterCustomerView` gains `balanceCents` and `amountToPayCents`; `TodaysRecordView` gains
  `paidCents`, `askedCents` and `balanceWithoutRecordCents`, and **loses `paid`** — the last US-29.3
  bridge in `application/` is gone. All five figures come out of the `recordsForCustomer` the lookup
  already loads for `recordForDay` and `countNoShows`: **no second query** (US-04.3).
- `balanceCents` is the balance *as it stands now*, today's hand-out included when one has been
  recorded; `amountToPayCents` is `amountToPay(allowance.priceCents, balanceCents)`.
  `balanceWithoutRecordCents` is `balanceOf` of the other rows, so the removal warning can name where
  a household would land (rule 9).
- `askedCents` is replayed, not looked up: a module-private `askedForOn` reduces over
  `replayPayments`, seeded with the record's own price — the same shape `correctAttendance` uses, and
  for the same reason (a `find(...) ?? fallback` would leave an arm nothing can reach, which the
  branch gate refuses).
- Nine tests written first and run **red** before a line of the implementation, including the four
  that pin the formula against a household whose today-price (2,00 €) differs from every hand-out
  behind them (5,00 €).
- The app-layer bridge moved rather than grew: `src/app/ausgabe/page.tsx` now reads the checkbox's
  `paid` back through the domain's own `standingOf(paidCents, askedCents) !== "SHORT"`. US-29.7
  removes it with the checkbox.
- Files changed: `src/application/customers/lookup-customer{,.test}.ts`, `src/app/ausgabe/page.tsx`,
  `scripts/ralph/prd.json`, this log.
- Gates: `npm run lint`, `npm run typecheck`, `npm run test:coverage` (52 files, 1217 tests, domain/
  and application/ still 100%), `npm run build`, `npx prettier --check .`, `npm run test:e2e`
  (177 passed) and `npm run test:e2e:webkit` (177 passed, run **three** times) — all green.
- **Learnings for future iterations:**
  - **Two WebKit failures in this iteration were pre-existing, and proving it took a `git stash`.**
    A full webkit run on clean `HEAD` failed the same settings spec, which is the only thing that
    separates "my change broke it" from "this suite is flaky here". Do that before touching a spec
    the story has no business in — and note that `npm run test:e2e:webkit` aborts the remaining
    files after a failure (`6 did not run`), so one flake hides whatever was behind it.
  - Both flakes were hazards this log already names, and both are now fixed in a commit of their
    own: a controlled `<textarea>` filled with a plain `fill()` straight after `goto`
    (`notes-field` — `fillSticky` is the helper for exactly that window), and two consecutive saves
    on the settings screen without a `page.reload()` between them. **`selectOption` does not
    reliably survive that race either**, contrary to what the patterns bullet said — it is now
    corrected there.
  - **Running one spec file against the shared register is not a free diagnostic.** Playwright
    reuses an existing server locally (`reuseExistingServer: !process.env.CI`) and each invocation
    deletes and re-seeds its own database, so a single-file run is honest — but the *order* it
    implies is not the order the full suite has, and a spec that asserts a shared count will read
    differently. Re-run the whole file set before concluding anything.
  - **`amountToPayCents` is defined for a household already served today**, and is what they *would*
    be asked for on a second hand-out. That is not a bug and not something to null out: the screen
    simply does not render it in that state (US-29.7), and the record's own `askedCents` is what the
    already-served half shows.
  - Docs still untouched — US-010 owns every document in this batch (same reasoning as US-001..004).
---

## 2026-08-28 — US-006 The customer record reads the balance and the payment history (application)
- `CustomerCardView` gains `balanceCents: Cents`, and `history` becomes
  `ReadonlyArray<Settlement<DistributionRecord>>` — each row now carrying `askedCents`, `standing`
  and `balanceAfter` beside the record itself. Both come out of the `records` `readCustomer` already
  loads for `countNoShows`: **no extra query**.
- The display order is `[...replayPayments(records)].reverse()`, one line, commented as being the
  *display* order rather than the arithmetic's — `replayPayments` must walk oldest first because a
  running balance can only be built that way.
- The price on each row is still the record's own, and `askedCents` is replayed from those same
  prices: a policy change since then rewrites nothing.
- **Nothing special-cases an archived household or a re-registered one**, which is the point — two
  named tests pin the properties (`keeps an archived household's balance`, `starts a re-registered
  household at zero`) because both are things somebody could break later, the second by keying
  records on the customer number instead of the surrogate id (ADR-008).
- Eight tests written first and run **red** (8 failing, including the adapted newest-first test)
  before a line of the implementation.
- `src/app/kunden/[id]/page.tsx` follows the type mechanically: `History` takes settlements and
  destructures `({ record })`. The US-29.3 „Bezahlt: ja/nein" bridge stays exactly as it was —
  US-008 owns the Gefordert/Gezahlt columns and the colour mark that replace it.
- Files changed: `src/application/customers/{read-customer.ts,customers.test.ts}`,
  `src/app/kunden/[id]/page.tsx`, `scripts/ralph/prd.json`, this log.
- Gates: `npm run lint`, `npm run typecheck`, `npm run test:coverage` (52 files, 1224 tests, domain/
  and application/ still 100%), `npm run build`, `npx prettier --check` on the touched files,
  `npm run test:e2e` (177 passed) and `npm run test:e2e:webkit` (177 passed) — all green.
- **Learnings for future iterations:**
  - **Widening a view's row type breaks its consumer's `.map` silently in the test file first.**
    `view.history.map((record) => record.date)` still typechecks against a `Settlement` only if the
    field names happen to collide — they do not here, so `tsc` caught it, but rename the lambda
    parameter (`handOut`) rather than leaving a `record` that is no longer one. The same rename in
    `page.tsx` is a destructure, `({ record })`, which keeps the body untouched.
  - **`balanceOf(records)` beside `replayPayments(records)` is two computations of one number, and
    that is fine here**: `balance.test.ts` already pins that the last `balanceAfter` equals
    `balanceOf` of the same rows, so the two cannot drift without a domain test failing. Reaching
    into the replay's last element instead would need a fallback for the empty history — an arm
    nothing can reach, which the branch gate refuses (the same reason `askedForOn` is a `reduce`).
  - **The `attend` helper in `customers.test.ts` now takes `paidCents`** (default 500, the seeded
    price). Any later story needing a household in debt or credit in the `readCustomer` block should
    use it rather than writing a bare `records.create`.
  - Docs still untouched — US-010 owns every document in this batch, as US-001..005 each recorded.
---

## 2026-08-28 — US-007 The counter collects an amount (presentation)
- The paid checkbox is **gone from both counter forms**, replaced by a `Betrag` text field pre-filled
  with the amount to pay (serve) or the stored payment (correction): `name="betrag"`,
  `inputMode="decimal"`, `autoComplete="off"`, contents selected on focus, in the German amount form
  `formatEuroAmount` writes and `parseEuros` reads — `4,00`, no currency symbol. Both forms took
  `onKeyDown={guardEnter}`, so Enter no longer submits either of them.
- A **payment row** above the serve form, in `ServeControls` rather than on the lookup card: `Zu
  zahlen` (`counter-amount-to-pay`, `text-4xl`) and `Saldo` (`counter-balance`), worded and never
  signed off `balanceKind`. The four derived tiles are untouched and the price is still fourth. The
  already-served card shows the `Saldo` alone — the amount to pay in that state is what a *second*
  hand-out would cost, which cannot happen.
- The overpayment step is **driven from the server's refusal**: the form submits without a
  confirmation, `OverpaymentNotConfirmed` comes back as the new `confirmOverpayment` status on both
  `ServeState` and `CorrectState`, and the notice renders `refusal`-tiered with a second submit
  button (`name="overpaymentConfirmed"`) beside it. No modal, nothing to dismiss.
- `de.distribution.serve.paid` and `paidState` **deleted**. New: `serve.amount`, `serve.asked(cents)`,
  `serve.overpayment.{question,confirm}`, `serve.errors.notAnAmount`, a reworded
  `alreadyServed(time, paidCents, askedCents)`, `correct.save` → „Betrag speichern", and
  `correct.removeConfirm(balanceWithoutRecordCents)`; plus `customers.derived.{amountToPay,balance,
  balanceValue}`.
- Files changed: `src/i18n/de.ts`, `src/app/ausgabe/{serve-controls.tsx,serve-state.ts,actions.ts,
  page.tsx}`, `src/app/enter-guard.ts`, `tests/e2e/serve.spec.ts`, `scripts/ralph/prd.json`, this log.
- Gates: `npm run lint`, `npm run typecheck`, `npm run test:coverage` (52 files, 1224 tests, domain/
  and application/ still 100%), `npm run build`, `npx prettier --check .`, `npm run test:e2e`
  (177 passed) and `npm run test:e2e:webkit` (177 passed) — all green. Browser pass with
  `playwright-cli` against `npm run start`, pinned to a BLUE distribution day over the demo register;
  console 0 errors, `scrollWidth - clientWidth` 0 at 1440/1280/800/390.
- **Learnings for future iterations:**
  - **React resets an uncontrolled form once its action resolves, and that silently broke the
    overpayment confirmation.** Typing 50,00 against 7,00 asked produced the right question — and the
    field snapped back to `7,00` behind it, so pressing „Ja, Betrag so buchen" would have booked 7,00
    with the flag. The browser pass is the only thing that found it; every gate was green. The fix is
    to drive `defaultCents` from the refusal (`overpayment?.paidCents ?? default`) **and key the field
    on it**, so a fresh input mounts on the refused amount whichever order React re-renders and resets
    in. Any form that has to survive its own action's answer needs the same treatment.
  - **`de.ts` now has imports, and it never had any before.** `formatEuros`, `balanceKind` and
    `BalanceKind`, all pure domain. The PRD asked for `balanceValue(kind, cents)` and `asked(cents)`
    by name, and formatting inside the dictionary is also what keeps a leading minus off every screen
    at once rather than leaving each caller to remember `Math.abs`. The precedent it breaks —
    `history.change(label, from, to)` takes ready-made strings — is left as it is.
  - **A dictionary entry may not read `de` inside its own initialiser.** `removeConfirm` wanted
    `de.customers.derived.balanceValue`, which makes `de` implicitly `any` (TS7022) even though it
    would work at run time. Module-level `balanceWording` above the object is the shape the egg-rule
    fragments (`eggRow`, `fromPersons`) already use for exactly this.
  - **`whitespace-nowrap` on a `Stat` value is a narrow-screen bug.** „Offen 2,00 €" ran out through
    the tile's own padding at 390px while `documentElement.scrollWidth - clientWidth` still read `0` —
    the page does not overflow, the *tile* does, and only the screenshot shows it. Pass the width floor
    where tiles are compared; let a phrase wrap where they are not.
  - **The payment row shares the counts row's four-track grid** (`grid-cols-2 sm:grid-cols-4`, `Saldo`
    spanning two), not a grid of its own two columns. The two cards are the same width with the same
    padding, so four tracks put `Zu zahlen` on the same baseline as `Erwachsene`; a bare `grid-cols-2`
    gave two half-width tiles that lined up with nothing above them.
  - **`playwright-cli` takes a testid as a bare target** (`playwright-cli fill serve-amount "50,00"`)
    only once the element is in the current snapshot — otherwise it errors with "does not match any
    elements" and a `snapshot` first is what fixes it, not a different locator. `eval` is how you read
    `selectionStart`/`activeElement`, which is the only honest check for select-on-focus and for
    keyboard reachability of a confirm button.
  - **`pkill -f "next-server"` kills the Bash tool** (exit 144, no output) exactly as this log's
    pattern bullet warns — and `ui_styling_guide.md` §11 still prints `pkill -f next-server` in its
    snippet. Kill by pid from `ps aux | grep "[n]ext-server"`.
  - **Driving the counter needs `FD_FIXED_NOW_FILE` on the server**, not just a seeded register: the
    demo day is deliberately not a distribution day, so no household is clear to serve. Pin a Thursday
    in a week whose colour matches the household you want (anchor `2026-W02` = RED, so odd offsets are
    BLUE) and restart `npm run start` with the variable set.
  - Docs still untouched — US-010 owns every document in this batch, as US-001..006 each recorded.
---
