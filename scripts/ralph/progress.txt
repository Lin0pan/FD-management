# Ralph Progress Log
Started: Fri Jul 24 14:11:17 CEST 2026
---

## Codebase Patterns
- Domain date rules compare **UTC calendar days** via a local `utcDay(date)` helper (`Date.UTC(y, m, d)`) — see householdComposition.ts and certificate.ts. Never compare raw timestamps; add a named "ignores the time of day" test.
- Domain test style: tiny ISO-string builders (`on("2026-08-31")`, `bornOn(...)`) returning `new Date(\`${iso}T00:00:00.000Z\`)`; one `it` per business rule, named after the rule.
- Boundary tests always cover: day before, day of, day after, plus 29 February (both directions where meaningful).
- `docs/technical_documentation.md` enumerates every module twice: in the source-tree listing (~line 90) and as a `### src/...` section — update both when adding a module.
- Coverage gate: 100% on domain/ and application/; type-only modules (e.g. distributionRecord.ts) show 0% but are excluded from thresholds.
- Ralph batch bookkeeping (archive dir, .last-branch, new prd.json) may sit uncommitted at iteration start — commit it as a separate `chore(ralph)` commit, not mixed into the feat.

## 2026-07-24 14:15 - US-001 (Certificate validity rule)
- Implemented `isExpired(certificate, today)` in `src/domain/customer/certificate.ts` (TDD, red→green): valid **on** `validUntil`, expired the day after, UTC-day comparison, no clock/settings. Reuses the existing `NeedsCertificate` type from customer.ts.
- Files changed: src/domain/customer/certificate.ts (new), src/domain/customer/certificate.test.ts (new, 7 tests), docs/technical_documentation.md (tree + module section).
- Quality: lint ✓, typecheck ✓, test:coverage ✓ (447 tests, domain at 100%), build ✓.
- **Learnings for future iterations:**
  - `NeedsCertificate` already exists in customer.ts (`{ type, validUntil }`) — don't invent a new certificate type in later US-06 stories.
  - `Customer.reminderCount` already exists on `NewCustomer` (US-01 schema) — US-06.3 only needs to confirm it, plus add the `ReminderLog` model.
  - No escalation/threshold anywhere is a hard non-goal (PRD §5) — resist adding one in application or UI stories.
  - Deliberate duplication: `utcDay` is private in both householdComposition.ts and certificate.ts; the project prefers boring duplication of a 3-line helper over a shared date module.
---
## 2026-07-24 14:25 - US-002 (recordReminder and renewCertificate use cases)
- Implemented `recordReminder(deps, {customerId})` (returns the new count) and `renewCertificate(deps, {customerId, type, validUntil})` in `src/application/customers/`, TDD red→green against hand-written fakes and a fake clock (14 tests).
- New ports in ports.ts: `ReminderLogRepository` (findOnDay/record; record is transactional entry+count, adapter reports lost race as `ReminderAlreadyLoggedToday`) and `CertificateRepository` (renew appends cert + resets count to 0 in one transaction). New domain errors: `ReminderAlreadyLoggedToday`, `CertificateStillValid`, `CertificateValidUntilInPast`.
- Files changed: src/application/customers/record-reminder.ts (new), renew-certificate.ts (new), certificate-reminder.test.ts (new), src/application/ports.ts, src/domain/errors.ts, docs/technical_documentation.md (tree, ports table, new module section).
- Quality: lint ✓, typecheck ✓, test:coverage ✓ (461 tests, application at 100%), build ✓.
- **Learnings for future iterations:**
  - The reminder day-key is the **Berlin** day via `berlinDayKey` from `@/domain/distribution/attendance` — US-06.3's `ReminderLog.loggedOn` column must be filled by the same function (`YYYY-MM-DD`), mirroring `DistributionRecord.dayKey`.
  - The reminder audit entry records the resulting count in `why` (`reminderCount=2`) — the AuditEntry shape has no other slot; the `why` doc in ports.ts was updated to name this one machine-written case. US-06.4's UI should not re-derive it.
  - `renewCertificate` accepts `validUntil` = today (valid through its last day, same `isExpired` rule); rejects blank type with `MissingRequiredField("certificate.type")` — matches registration's field naming.
  - Current Prisma `Certificate` model is one-per-customer (`customerId @unique`, "renewal replaces the dates") — US-06.3 must flip it to appended rows + `recordedAt`, update the model comment, and regenerate the pre-release migration (delete prisma/migrations, `npx prisma migrate dev --name init`, re-add the hand-written partial unique index for customerNumber!, then `npm run db:reset`).
  - New DomainError classes don't need errors.test.ts entries when application tests construct them — domain coverage stays 100% through those.
---
