# Ralph Progress Log
Started: Sun Jul 26 16:22:01 CEST 2026
---

## Codebase Patterns
(Carried forward by hand from the US-09 log — the archive rotation wipes this file, so the patterns
block has to be pasted into the fresh one. The other durable channel is the CLAUDE.md files.)
- Adding a method to a port interface (e.g. CustomerRepository) forces EVERY implementer to compile:
  the real Prisma adapter AND the hand-written fakes, which live in several test files —
  customers.test.ts, certificate-reminder.test.ts, lookup-customer.test.ts and
  distribution/record-attendance.test.ts. `grep -rln "implements <Port>" src/` before you start.
  Miss one and typecheck fails, not lint.
- Adding a field to RegisteredCustomer ripples the same way: every fake `create` returning
  `{ ...customer, id }` and every builder literal needs the new field.
- `issueCard` (application/customers/issue-card.ts) is the ONE path by which a card comes into
  existence; `reissueCard` delegates to it and only narrows the reason. Never write a second issuing
  path — validity is "holds the highest index", so a parallel path is how two valid cards appear.
- Prisma migrations are pre-release disposable, but ADDITIVE changes (a new nullable column) get
  their own migration via `npx prisma migrate dev --name <x> --skip-generate`, then
  `npx prisma generate`. Infra tests spin a throwaway DB with `prisma migrate deploy`, so only a
  committed migration makes them see the column.
- Status changes go through `transition(from, to, reason?)` in domain/customer/status.ts, never an
  ad-hoc status assignment; persist via CustomerRepository.setStatus — except the archive, which has
  its own `archive(id, reason, archivedAt)` write because it stamps three columns and clears
  blockReason in one statement.
- Derive-don't-store: household counts, ages, card validity and the card number are all derived on
  read (readCustomer / readCard / describeAllowance), never stored.
- German UI strings only in `src/i18n/de.ts`; inflected forms are functions in the dictionary.
- Coverage is gated at 100% on domain/ and application/ — any new branch needs a named test.
- e2e specs (tests/e2e/*.spec.ts) do not run under `npm run test:coverage`; run `npm run test:e2e`.
  Its webServer is `npm run start` (the BUILT app) with `reuseExistingServer`, so an app-layer change
  needs `npm run build` first or the browser sees old code.
- e2e specs seed straight through Prisma and navigate by the SURROGATE id (`/kunden/${id}`), not the
  customer number. Owned number ranges keep the shared data/e2e.db predictable: counter 201-206/239,
  portions 211, serve 221-222, reminders 231, block 241, reissue 251 — so US-10 takes 261 up. Never a
  number the allocator would hand out, or the registration specs' assertions move.
- Assert that a refusal wrote NOTHING by bracketing it with one Prisma snapshot helper returning
  `JSON.stringify({ status, cards, records, auditEntries })` and comparing before/after, rather than
  asserting each field. One equality covers every table the screen could have touched, and a future
  write that nobody thought to check still fails the spec.
- Assert German UI text in e2e specs by importing `de` from `@/i18n/de` and calling the dictionary
  function (`de.customers.reissue.confirm(a, b)`), never by pasting the sentence. A reworded string
  then stays a one-file change instead of breaking specs.
- App-layer write screens follow one shape: a `"use server"` `actions.ts` (Zod-parse the hidden
  `customerId`, call ONE use case, map typed domain errors to German, `revalidatePath`), a
  `*-state.ts` holding the form-state type + initial value (a `"use server"` module may export only
  async functions), and a `"use client"` component using `useActionState`. `customerDeps` already
  bundles customers+cards+settings+clock+audit and satisfies most use-case Deps structurally.
- The customer record page is `/kunden/[id]` (page.tsx + block-controls.tsx + reissue-controls.tsx);
  the card view is `/kunden/[id]/karte`. Extend them rather than adding pages. One client control can
  serve two screens — its action should `revalidatePath` both.
- A count the UI shows belongs in the repository as an aggregate, not as a `.filter()` over a list
  the read model already holds: the filter is a second statement of the rule and drifts the day a
  case is added. `prisma.card.groupBy` with `_count`/`_max` is the shape — one round trip.
- A number the confirmation text of a write screen has to name (the next card number, a computed
  total) belongs on the READ MODEL, not in the page: `app/` is thin and "the next index is the
  current one plus one" is a rule.
- Reason/status/group words come out of SQLite as plain strings and are narrowed by a `parse*`
  function (`parseCardIssueReason`) that THROWS `InvalidCustomerRecord`. Do this in aggregates too —
  a bare `=== "LOST"` would let a hand-edited row silently vanish from a count.
- "The same day" is ALWAYS `berlinDayKey` (domain/distribution/attendance.ts) — never a UTC date and
  never a 24-hour window. Week colours and distribution days, by contrast, are UTC day instants
  (`startOfUtcDay`). Mixing the two is the standing trap in domain/distribution.
- There is no registration-date column. A household's start is its FIRST card (`index` 1,
  `FIRST_ISSUE`), written with the registration — that is where `registeredOn` comes from.
- Prettier reformats markdown TABLES and wraps long `expect(...)` chains on `npm run format`; run it
  before committing so `format:check` in CI does not fail on whitespace.
- A rule about WHICH state changes need a reason belongs in `transition()` (domain/customer/status.ts),
  not in the use case: the machine refuses the move and names the audit event
  (`customer.blocked`, `customer.archived`) so the caller never restates it. A use case that
  re-checked the reason would be a second statement of the rule.
- A use case proves "nothing was deleted" by taking ONE JSON snapshot of everything the household
  owns (cards, records, number, group, reminderCount, details) before and after and comparing them —
  the same trick the e2e specs use. Repositories the use case does not depend on are handed to the
  test's `deps()` anyway as witnesses; excess-property checking does not apply because `deps()`
  returns a variable rather than an object literal at the call site.

---

## 2026-07-26 - US-001 (consecutiveNoShows counting rule)
- `src/domain/distribution/noShows.ts` (new): `consecutiveNoShows({ records, customerGroup,
  registeredOn, settings, today })` walks back one two-week cycle at a time from the last distribution
  of the customer's OWN colour and stops at the first one they attended, or at their registration day
  (tasks/prd-us-10-archive-customer.md §US-10.1). Pure — `today`, `settings` and `registeredOn` are
  parameters, no clock, no I/O.
- The four boundaries, each a decision rather than an implementation detail:
  - the other group's weeks are not candidates (they were never this household's days);
  - TODAY's own distribution never counts — it may still be in progress, and a count that included it
    would read "1 no-show" to the staff member serving the customer;
  - the registration DAY itself never counts: the card is handed over at registration and whether that
    day's hand-out had already finished is nowhere on record;
  - weeks the customer was BLOCKED do count (PRD §9, still to confirm with FD) — excluding them would
    hide the pattern the count exists to show, and it would need a block *history*, which the record
    does not keep. The decision is a comment in the module and a named test.
- Records are matched to a candidate day by `berlinDayKey`, the same notion of "the day" as the
  once-per-day attendance rule and the DB's unique index. Candidate days are UTC midnights, which map
  to the same Berlin date year-round (Berlin is always ahead of UTC), so the two agree.
- `lastOwnDistributionBefore` stayed PRIVATE rather than becoming a `previousDistribution` sibling in
  distributionDay.ts: `nextDistribution` there is inclusive of today, and a neighbour that excluded it
  would be a trap. The exclusion belongs to this rule.
- 17 tests in `noShows.test.ts`: never attended (0 and 3), attended last time, missed one, missed
  three, stops at the most recent attendance rather than the longest gap, the other group's weeks
  ignored, today not counted, the day after a missed distribution, registered mid-cycle, registered ON
  a distribution day, a group change mid-history (counted on the NEW group's schedule), blocked weeks
  counted, a 23:45-Berlin attendance matched, an after-midnight one not, 29 February crossed without
  drift, and the time of day of `today` ignored.
- Files changed: src/domain/distribution/noShows.ts (new), src/domain/distribution/noShows.test.ts
  (new), docs/technical_documentation.md (repo-layout tree + a module section with the boundary
  table), scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Checks: format:check, lint, typecheck, test:coverage (553 tests, 34 files, 100% on
  domain/application incl. the new module), build — all green. No UI in this story, so no browser
  check applies.
- **Learnings for future iterations:**
  - Written TDD-first: the test file was committed red (module missing) before a line of the module
    existed, which is what the domain layer's rule demands.
  - The 2026 Thursday/colour table is worth copying into the next spec that needs one: with anchor
    `2026-W02` RED and Thursdays, RED falls on 01-08, 01-22, 02-05, 02-19, 03-05 and BLUE on 01-01,
    01-15, 01-29, 02-12, 02-26. 2028-03-02 is a RED Thursday, which is how the 29 February case is
    reached.
  - Next up, US-002 (`archiveCustomer`): reuse `MissingAuditReason("customer.archived")` from
    errors.ts — do NOT add an error class — and go through `transition()` in domain/customer/status.ts.
    The test has to prove the freed number comes back from `lowestFreeNumber` and that no row anywhere
    was removed; count rows before and after on the fakes rather than trusting the type.
  - US-003 then adds `archiveReason`/`archivedAt` columns (additive migration) and US-004 needs
    `registeredOn` on the read model — derive it from the first card's `issuedAt`, since no
    registration-date column exists.
---

## 2026-07-26 - US-002 (archiveCustomer use case)
- `src/application/customers/archive-customer.ts` (new): `archiveCustomer(deps, { customerId,
  reason })` trims the reason, asks `transition()` to move `ACTIVE|BLOCKED → ARCHIVED`, then writes
  it through one new port call and appends the audit entry (`customer.archived`, changed fields
  `status`/`archiveReason`/`archivedAt`, the reason as the why).
- TDD order was: domain test red → `transition()` green → application test red (module missing) →
  use case green → adapter and fakes.
- The mandatory reason went into the STATE MACHINE, not the use case (this is the one design call of
  the story). `REASON_REQUIRED` maps the reason-carrying target states to their audit event names, so
  `→ BLOCKED` and `→ ARCHIVED` are both refused reason-less with `MissingAuditReason` naming the
  event. errors.ts already documented the archive as one of its raisers; the use case would have been
  a second statement of the rule. Two existing status tests changed (`transition("ACTIVE",
  "ARCHIVED")` now needs a reason) — a deliberate behaviour change, not a fix.
- New port method `CustomerRepository.archive(id, reason, archivedAt)` rather than a wider
  `setStatus`: the archive writes three columns and clears `blockReason` in the same statement, and
  the invariant `(blockReason !== null) === (status === "BLOCKED")` that the infra tests pin has to
  survive archiving a blocked customer.
- Because a port method forces every implementer to compile, this story also carried what US-003
  lists: `Customer.archiveReason String?` / `archivedAt DateTime?`, the additive migration
  `20260726145943_customer_archive_fields`, and `archiveReason`/`archivedAt` on `RegisteredCustomer`.
  US-003 still owns the repository invariant tests, the no-cascade check and the `(customerId, date)`
  index — note that `takenActiveNumbers` excluding archived rows and two archived customers sharing a
  number are ALREADY covered in customer-repository.test.ts (lines ~196 and ~277).
- Four fakes needed the new method and the two new fields: customers.test.ts,
  lookup-customer.test.ts, certificate-reminder.test.ts, distribution/record-attendance.test.ts —
  plus the two `RegisteredCustomer` builder literals in the last two.
- 11 application tests: reason trimmed and stamped, the freed number offered by `lowestFreeNumber`
  on the next call, the number kept on the archived row, one before/after snapshot proving nothing
  was deleted, a blocked customer archived with the block reason cleared, the audit entry, an empty
  and a whitespace reason refused writing nothing, the slot still taken after a refusal, an
  already-archived customer refused, and an unknown id refused. 4 infra tests cover the adapter.
- Files changed: src/application/customers/archive-customer.ts (new), src/application/ports.ts,
  src/domain/customer/status.ts, src/domain/customer/customer.ts, the four fake-holding test files,
  src/infrastructure/prisma/customer-repository.ts(+.test.ts), prisma/schema.prisma, the new
  migration, docs/technical_documentation.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Checks: format, lint, typecheck, test:coverage (571 tests, 34 files, 100% on domain/application),
  build — all green. No UI in this story, so no browser check applies; the screens arrive in US-004.
- **Learnings for future iterations:**
  - US-004 now has everything it needs on the read path: `RegisteredCustomer` carries
    `archiveReason`/`archivedAt` and the adapter maps them, so the archived banner is a read model
    field away rather than a new query. `readCustomer`'s read model does NOT expose them yet.
  - The app-layer action for US-004 follows the block precedent exactly: `[id]/actions.ts` →
    `archiveCustomer`, a `*-state.ts` for the form state, a client control beside block-controls.tsx.
    `customerDeps` already satisfies `ArchiveCustomerDeps` structurally.
  - `MissingAuditReason` is thrown from the domain now, so the app layer maps it to German once and
    both the block and the archive screens get the same message.

## 2026-07-26 - US-003 (Archive fields and no-delete invariants)
- Three of the six criteria were already met by US-002's spillover: `archiveReason`/`archivedAt`, a
  committed migration, and the two number-invariant tests (`takenActiveNumbers` skips archived rows,
  customer-repository.test.ts ~329; two archived rows may share number 50, ~196). The real work was
  the fourth: **no cascading deletes anywhere**.
- `HouseholdMember`, `Certificate` and `Card` still carried `onDelete: Cascade`; the two younger
  models (DistributionRecord, ReminderLog) never did. All five foreign keys are now
  `ON DELETE RESTRICT`, so SQLite refuses to delete a household that owns anything. The reasoning is
  in schema.prisma on `Customer` and in docs/technical_documentation.md §5: a cascade is harmless
  only for as long as nothing calls `delete`, which is exactly why it should not be left lying about
  — the day a clean-up script or a mistyped test calls it, the history goes silently and the audit
  log cannot say what was lost.
- **The migration history was collapsed, not extended.** Removing a cascade contradicts `init`, and
  CLAUDE.md's pre-release rule is that such a change *replaces* it: `rm -rf prisma/migrations`,
  `npx prisma migrate dev --name init --skip-generate`, re-append the hand-written partial unique
  index (Prisma cannot express it and regenerating drops it — schema.prisma says so at the top),
  `npx prisma generate`, `npm run db:reset`. Three migrations became one
  `20260726150653_init`. **Anyone with an existing checkout must run `npm run db:reset`** or
  `migrate deploy` reports a history that does not match.
- The delete refusal has a price: `prisma.customer.deleteMany()` in a `beforeEach` is now a foreign
  key error in any spec that registered a household. `clearRegister` in the new
  `src/infrastructure/prisma/test-support.ts` states the children-first order once and all five
  repository specs call it.
- New `src/infrastructure/prisma/schema.test.ts` (5 tests, no database — reads the files): no
  `onDelete: Cascade`/`SetNull`/`SetDefault` in the schema, no `ON DELETE CASCADE` in any committed
  migration, and the two hand-written/US-05.3 indexes still present. Comments in schema.prisma say
  the words "onDelete: Cascade" on purpose, so the test strips comment lines first.
- Three new repository tests prove the refusal against a real database: a delete is rejected with
  `Prisma.PrismaClientKnownRequestError`, it takes nothing with it (members/certificates/cards
  counted afterwards), and it is refused just the same once the household is archived.
- Files changed: prisma/schema.prisma, prisma/migrations/ (three deleted, one added),
  src/infrastructure/prisma/test-support.ts (new), schema.test.ts (new), the five repository specs,
  CLAUDE.md (the hard-delete Don't now names the rule and the helper),
  docs/technical_documentation.md (§2 tree, §5 — the no-cascade note moved from two per-model
  mentions to one rule about the whole schema), scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Checks: format, lint, typecheck, test:coverage (579 tests, 35 files, 100% on domain/application),
  build, and test:e2e (36 passed, 1 skipped) — all green. e2e was run deliberately: it rebuilds
  data/e2e.db from the migrations, so it is what proves the regenerated init still carries the
  partial unique index. No UI in this story.
- **Learnings for future iterations:**
  - After ANY migration-history rewrite, run `npm run test:e2e` as well as the unit suite. The unit
    infra specs migrate a throwaway file, but only the e2e run exercises `migrate deploy` against a
    seeded database the app then reads — the same path a developer's checkout takes.
  - `prisma migrate dev` refuses to regenerate while `data/fd.db` still has the old history applied;
    delete the file first (the one-liner from `db:reset`) rather than answering its reset prompt,
    which the sandbox cannot do.
  - Prisma writes `ON UPDATE CASCADE` on every foreign key by default and there is no schema syntax
    to turn it off. That is not a delete cascade and ids are never updated, so it is left alone —
    assert on `ON DELETE CASCADE` specifically, or the guard test fails on Prisma's own output.
  - US-004 (the UI) needs the no-show count on the read models. `consecutiveNoShows` wants the
    customer's distribution records; `DistributionRecordRepository.listForCustomer(id)` already
    exists and is backed by the `(customerId, date)` index, so no new query is needed —
    `registeredOn` still has to come from the first card's `issuedAt`.
