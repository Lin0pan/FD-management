# Ralph Progress Log
Started: Thu Jul 23 15:09:52 CEST 2026
---

## Codebase Patterns
- Domain modules are pure and take dates as parameters — `new Date()` with no argument is an ESLint
  error in `src/domain` and `src/application`. `new Date(value)` is fine.
- Calendar rules work on **UTC day instants** (`Date.UTC(y, m, d)`), never on raw timestamps or local
  time — see `householdComposition.ts` and `distribution/weekColour.ts` for the idiom.
- Coverage on `src/domain` and `src/application` is gated at **100% including branches**. Every
  branch of a helper needs a named test, so do not add a defensive branch you cannot reach.
- Typed errors from `src/domain/errors.ts` only — add a code to `DomainErrorCode` before the class.
- Test names state the business rule, not the function: "flips on the Sunday-to-Monday boundary".
- `docs/technical_documentation.md` carries both a source tree and a per-module section; a new domain
  module needs an entry in each.
- Full gate: `npm run lint && npm run typecheck && npm run test:coverage && npm run build`.
- Shared calendar arithmetic lives in `distribution/weekColour.ts` (`startOfUtcDay`, `isoWeekdayOf`) —
  import it rather than re-deriving UTC/ISO weekday maths in a new module.
- Verify every ISO date claim in a test comment against a real calendar before trusting it; an
  expectation written from intuition is the likeliest red in this area.
- A use case that answers about a **past** date resolves settings with `resolveSettingsAt(versions,
  thatDate)` rather than through `readCurrentSettings` — the latter always resolves at `clock.now()`
  and would apply today's policy to a historic question.
- Application use cases return a view object carrying everything one screen states, so the page needs
  a single call; `app/` composing two use cases is where logic leaks back up.
- A route is three files at most: `deps.ts` (composition root, real adapters), `page.tsx` (server
  component), and a client component **only** when `useActionState` or live typing demands one. A
  read-only screen with a GET form needs no `"use client"` at all.
- Every screen needs its own `deps.ts` naming only the ports it uses — do not import another route's.
- `npm run dev` rewrites `next-env.d.ts` to the `.next/dev/...` types path; `npm run build` puts it
  back. Check `git status` after driving the dev server and `git checkout next-env.d.ts`.
- A throwaway script that imports `@prisma/client` must live **inside the repo** (`./x.tmp.ts`), not
  in `/tmp` — module resolution is relative to the file. Use `async function main()`; `npx tsx -e`
  has no top-level await.
- Reset the dev database with `npm run db:reset` after poking at it to see a screen branch render.

---

## 2026-07-23 - US-001 (weekColour calendar alternation rule)
- Implemented `src/domain/distribution/weekColour.ts`: `colourOf(date, anchor)` derives the RED/BLUE
  week colour by ISO-week parity from the configured anchor, and `isoWeekOf(date)` formats the ISO
  week as `2026-W30` for the lookup UI (US-004 will need it).
- Written test-first: the spec was red (module missing) before the implementation existed.
- Files changed:
  - `src/domain/distribution/weekColour.ts` (new)
  - `src/domain/distribution/weekColour.test.ts` (new, 23 tests)
  - `docs/technical_documentation.md` (source tree + a module section)
  - `scripts/ralph/prd.json` (US-001 passes)
- Gate: lint, typecheck, `test:coverage` (310 tests, 100% on domain + application) and `build` all
  green.
- **Learnings for future iterations:**
  - ISO facts verified against a scratch script rather than intuition, as the PRD demands:
    2026 is a 53-week ISO year (1 Jan 2026 is a Thursday); 1 Jan 2023 falls in `2022-W52`;
    1 Jan 2027 falls in `2026-W53`; `2026-W02` starts Monday 5 January 2026.
  - The ISO year of a week is the calendar year of its **Thursday** — that single rule makes both the
    December and the January crossovers fall out; and 28 December is always in the last week of its
    ISO year, which is how `weeksInIsoYear` is computed.
  - Negative parity needs `((n % 2) + 2) % 2`; a plain `n % 2` is negative in JS and would make every
    pre-anchor week report the anchor colour.
  - `createSettings` validates only the *shape* of `weekAnchor.isoWeek` (a regex allowing W01–W53).
    `2025-W53` therefore passes settings validation but names no real week, so `weekColour.ts`
    re-checks existence and raises `InvalidSettings` against the same field. **Open follow-up:** the
    settings form (US-14) can still save such an anchor, and it would only fail when `/ausgabe`
    renders. Worth tightening in `createSettings` when US-014 is revisited — it was left alone here
    to keep this story to one intent.
  - `tsconfig.json` does **not** set `noUncheckedIndexedAccess`, so `RegExpExecArray[1]` is `string`
    and needs no guard — a guard would be an uncovered branch and fail the coverage gate.
  - There is no `e2e/` directory yet; `npm run test:e2e` runs Playwright from `tests/e2e` per
    `vitest.config.ts`'s comment. US-005 will have to create it.
---

## 2026-07-23 - US-002 (isDistributionDay and nextDistribution)
- Implemented `src/domain/distribution/distributionDay.ts`: `isDistributionDay(date, weekday)` and
  `nextDistribution(date, settings)` returning `{ date, colour }` for the next distribution at or
  after the given date — today when today is a distribution day.
- Written test-first: the spec was red (module missing) before the implementation existed.
- `weekColour.ts` grew two exports, `startOfUtcDay` and `isoWeekdayOf`, replacing its two private
  helpers so the new module reuses the same UTC/ISO arithmetic instead of duplicating it.
- Files changed:
  - `src/domain/distribution/distributionDay.ts` (new)
  - `src/domain/distribution/distributionDay.test.ts` (new, 13 tests)
  - `src/domain/distribution/weekColour.ts` (helpers exported)
  - `docs/technical_documentation.md` (source tree + a module section)
  - `scripts/ralph/prd.json` (US-002 passes)
- Gate: lint, typecheck, `test:coverage` (323 tests, 100% on domain + application) and `build` all
  green.
- **Learnings for future iterations:**
  - `date.getUTCDay()` is unaffected by the time of day, so the ISO weekday needs no day-normalising
    first — only the returned *date* does.
  - "Next occurrence of weekday W, today counting" is `(W - isoWeekday(d) + 7) % 7` days ahead; the
    `% 7` is what makes a distribution day return itself rather than +7.
  - One test failed first time because its comment mis-stated the calendar: 2025-W52 (22–28 Dec) is
    **two** weeks before the anchor 2026-W02, not one — 2025-W53 does not exist, so W52 is followed
    by 2026-W01. Check the week numbering, not just the day count.
  - `Settings.distributionWeekday` is already narrowed to `IsoWeekday` by `createSettings`, so the
    domain functions can take the narrow type and need no re-validation branch (which would be an
    uncovered branch under the 100% gate).
  - Next up (US-003) needs only `SettingsRepository` + `Clock`; the fake-based application tests in
    `src/application/settings/settings.test.ts` are the pattern to copy.
---

## 2026-07-23 - US-003 (getWeekColour use case)
- Implemented `src/application/distribution/get-week-colour.ts`: `getWeekColour(deps, date?)` resolves
  the settings version in force at the asked-about date and delegates to `colourOf`, `isoWeekOf`,
  `isDistributionDay` and `nextDistribution`. It returns a `WeekColourView` — the day, its ISO week,
  the colour, whether FD distributes that day and the next distribution — which is exactly what the
  US-004 banner has to state, so the page will need one call and no logic of its own.
- Written test-first: the spec was red (module missing) before the implementation existed.
- Only `SettingsRepository` + `Clock`; nothing is persisted, per FR-2/US-03.3.
- Files changed:
  - `src/application/distribution/get-week-colour.ts` (new)
  - `src/application/distribution/distribution.test.ts` (new, 8 tests)
  - `docs/technical_documentation.md` (source tree + an application module section)
  - `scripts/ralph/prd.json` (US-003 passes)
- Gate: lint, typecheck, `test:coverage` (331 tests, 100% on domain + application) and `build` all
  green.
- **Learnings for future iterations:**
  - `readCurrentSettings` is *not* reusable for a dated lookup: it hard-codes `clock.now()`. FR-6
    wants the anchor in force **then**, so this use case calls `resolveSettingsAt` on the history
    itself. Any future "what did it cost / what colour was it" question needs the same shape.
  - Settings are resolved at the asked-about **instant**, but the calendar arithmetic runs on
    `startOfUtcDay`. Resolving at day-start instead would hide a settings change saved earlier the
    same morning, which contradicts "a saved change is in force immediately" in CLAUDE.md.
  - The past-vs-present anchor test is only meaningful if the two dates sit an **even** number of
    weeks apart from the anchor — then the colours differ solely by the anchor colour in force.
    2026-01-08 and 2026-07-23 are 28 weeks apart, which is why that pair was chosen.
  - The application test file is named after the folder (`distribution.test.ts`), matching
    `customers.test.ts` and `settings.test.ts`; the fakes are re-declared per file rather than shared.
  - Next up (US-004) is the `/ausgabe` route: it does not exist yet under `src/app/`, and every string
    it renders must be added to `src/i18n/de.ts` (German literals in components are a lint error).
---

## 2026-07-23 - US-004 (Week colour banner and week lookup on /ausgabe)
- Implemented the distribution screen at `/ausgabe`: a dominant banner stating today's colour, and a
  date lookup for any past or future week showing the ISO week alongside. The screen calls
  `getWeekColour` once for today and, when `?datum=` is present, once for that day; it decides
  nothing itself.
- The banner is painted in the colour it *names*. On a non-distribution day that is the **next**
  distribution's colour — not the current week's, which would state a colour nobody collects in.
  The colour is always written out ("Gruppe Rot") as well as painted, per the accessibility criterion.
- Files changed:
  - `src/app/ausgabe/page.tsx` (new)
  - `src/app/ausgabe/deps.ts` (new — only `SettingsRepository` + `Clock`)
  - `src/i18n/de.ts` (`de.distribution.*`, `de.home.distributionLink`)
  - `src/app/page.tsx` (link to the new screen so it is reachable)
  - `docs/technical_documentation.md` (source tree, status line, a route section)
  - `scripts/ralph/prd.json` (US-004 passes)
- Gate: lint, typecheck, `test` (331 tests, unchanged — this story adds no pure logic) and `build`
  all green.
- **Verification:** no browser MCP tool is configured in this environment, so the screen was driven
  through `next dev` on port 3111 and the rendered HTML inspected. Confirmed on the seeded database
  (today 23.07.2026, a Thursday):
  - distribution day → "Heute ist Ausgabe / Gruppe Rot / 23.07.2026 · Kalenderwoche 2026-W30",
    banner `bg-red-600`;
  - `?datum=2026-07-30` → "Gruppe Blau … 2026-W31" (the alternation, visible on screen);
  - `?datum=2028-07-20` → "Gruppe Rot … 2028-W29" (two years out, per US-005's criterion);
  - `?datum=quatsch` → the German "not a date" sentence, banner intact;
  - `?datum=2001-01-01` → the German "no settings" sentence, banner intact;
  - after temporarily adding a settings version with `distributionWeekday: 5`, the off-day branch
    rendered exactly the PRD's sentence: "Heute ist keine Ausgabe … Nächste Ausgabe: 24.07.2026,
    Gruppe Rot". The database was reset afterwards.
- **Learnings for future iterations:**
  - The lookup needed **no client component**: a `method="get"` form puts the day in the URL, which
    is both simpler than `useActionState` and better behaviour — a checked colour can be reloaded or
    passed on as a link. Reach for `"use client"` only when state has to survive without a round trip.
  - `searchParams` is a `Promise` in Next 16 and its values are `string | string[] | undefined`. Type
    it honestly and reject the array case; do not cast.
  - A failing *lookup* must not take the *banner* down. Two separate `try` blocks, and the lookup
    returns a German sentence instead of throwing — a staff member asking about 2001 should still see
    what today is.
  - `getWeekColour` throws `NoSettingsInForce` for any date before FD's first settings version. That
    is reachable from the UI the moment someone picks an early date, so it is a normal answer on this
    screen, not an error page.
  - US-005 (E2E) needs a **test-only fixed clock**: `distributionDeps.clock` is `systemClock`, so the
    banner assertions cannot be pinned without one. An env variable read in `src/infrastructure/
    clock.ts` is the seam — `deps.ts` should keep choosing `systemClock` by name.
  - The e2e specs run against `data/e2e.db` (there is already one), so US-005 can seed a known anchor
    without disturbing the dev database.
---
