# Ralph Progress Log
Started: Mon Aug 24 2026 (batch 28 — US-28 the egg allowance)

## Codebase Patterns
- **Adding a `DomainErrorCode` is a three-file change**: the union in `src/domain/errors.ts`, the
  class beside it, and `TIERS` in `src/app/notice-tier.ts`. `TIERS` is a `Record` over the union, so
  the *build* fails until the code is tiered — that is the mechanism, not an obstacle. The header
  comment of `notice-tier.ts` counts the codes ("a 32nd code…", "the six red ones"), so it goes
  stale silently; update it with the entry.
- **The same mechanism guards `SettingsChange`**: its `switch` in `src/app/einstellungen/page.tsx`
  is exhaustive with no `default`, so adding a member to the union fails the build until the screen
  renders it. Do not silence it with a `default` branch.
- **An error class with no caller yet still needs a test**, because `domain/` coverage is gated at
  100%. `src/domain/errors.test.ts` is where those live — see the `MissingAuditReason` block for the
  shape, including the comment saying which story will call it.
- **`errors.ts` cannot import from `domain/card/`**: `cardNumber.ts` imports `errors.ts`, so a
  message that wants to read `66k1` writes the `k` inline rather than calling `formatCardNumber`.
- **Adding a field to `Settings` is a wide change before it is a deep one.** `createSettings` is
  called from the repository, the seed, the settings server action and every hand-written fake in
  the application tests; all of them must supply the new field before anything compiles. Land the
  field with a placeholder value at each call site, then replace them in the story that owns each.
  Get the *files* out of a wide break rather than the errors:
  `npm run typecheck 2>&1 | grep -E "^src/" | cut -d'(' -f1 | sort -u`.
- **A test that merely *uses* a field is not a test *of* it.** Several `settings.test.ts` cases
  reach for an arbitrary second field to prove the same-instant tie rule or the audit's changed-field
  list. When that field goes away, **re-point them**, do not delete them — deleting takes an
  unrelated rule with it and drops coverage below the gate.
- **A Prisma `P2002` cannot always name *which* constraint was broken.** `meta` is
  `{ modelName, target: [columns] }`, but where two unique indexes both cover the offending row
  SQLite reports only one of them, and not the one that was declared or created first. Where the
  distinction matters, match the target for the *shape* and then ask the database the question that
  actually distinguishes the two faults (`card-repository.ts`'s `issue` counts the customer's own
  rows on that index). For a **nested** write Prisma reports the *rooted* model — a card refused
  inside `customer.create` comes back as `modelName: "Customer"` — so the model name alone tells
  nothing about which table failed.
- **Regenerating `prisma/migrations/` drops the hand-written partial unique index** on
  `Customer.customerNumber` (`Customer_customerNumber_onRegister_key`) at the end of the migration
  SQL. `src/infrastructure/prisma/schema.test.ts` greps for it and for the `onDelete: Restrict`
  clauses, so a lost line fails CI rather than production — but re-add it deliberately. Three things
  batches 25–27 each had to learn:
  - **Delete `data/fd.db` before `migrate dev`**: with the old migration row still in
    `_prisma_migrations`, Prisma refuses to regenerate and writes nothing.
  - **Verify with `diff`, not by reading the new file**:
    `git show HEAD:prisma/migrations/<old>/migration.sql > /tmp/old.sql && diff /tmp/old.sql <new>`
    states in a few lines what the story is allowed to have changed. Anything else is a Prisma
    version drifting the DDL under you, which no gate would attribute to this commit.
  - **`migrate dev` applies the migration *before* the hand-written index is appended**, so the
    database it leaves does not have the index even though the committed SQL does. `npm run db:reset`
    afterwards is what makes the two agree — check with
    `SELECT sql FROM sqlite_master WHERE name='Customer_customerNumber_onRegister_key'` rather than
    trusting that the reset ran.
  - A regeneration **renames a directory that documents cite by name**. `grep -rn "<old-timestamp>_init"
    docs/ src/ tests/` afterwards; prefer the `prisma/migrations/*_init/migration.sql` glob, which
    survives the next one.
- **Never `pkill -f "next dev"`, and never `pkill -f "node.*next-server"`** — both patterns match the
  Bash tool's own wrapper command line and kill the shell (exit 144, no output). **And `ss` is not
  installed in this sandbox**, so `ss -ltnp | grep :3000 || echo free` reports "free" for every port,
  always: the `||` fires on `command not found`. (Batch 27's per-story notes recommend `ss` in two
  places — they are wrong; that is the bug this bullet exists to stop.) Check
  `ps aux | grep "[n]ext-server"` — the bracket keeps the pattern out of its own match — and
  `kill <pid>` by number.
- **A stale `next start` from an earlier iteration silently serves a mixed build.** The new
  `npm run start` fails with `EADDRINUSE` in the background while the page still renders, from the
  old server reading the *new* chunks off disk — a couple of console 500s are the only symptom.
  Check for a live server (`ps`, above) **before** trusting any browser check.
- **`npm run db:reset` does not reach a server that is already running.** It deletes `data/fd.db`,
  and SQLite keeps serving the running process from the deleted inode, so the screen goes on showing
  the pre-reset data. Restart the server after a reset, or the verification is of the old database.
  `db:seed` fills settings only — customers are not seeded, so a reset empties whatever manual
  clicking built up (the e2e suite makes its own registers).
- **Changing a seeded policy value moves e2e expectations.** `grep -rn ',00 €' tests/e2e/` before
  editing a number in `seed.ts`.
- **The e2e customer-number bands, as they now stand.** Below the quota of 240: counter 201–206 and
  239, allowance 211, serve 221–222, reminders 231, registration 232–234, card numbers 236. Above it:
  block 241, reissue 251, age-13 271, customer-list 281–285, customer-record 291, group-progress
  301–303, group-walk 311–315, price cap 321. A spec whose number must be **pickable in the
  /kunden/neu dropdown** has to sit below 240 — the pool is `1..quotaN` — and everything else should
  stay above it, clear of the low sequence the allocating specs (registration, card, archive,
  reregistration) consume. **A new spec takes a new band**; do not borrow another spec's numbers.
- **e2e spec files run in alphabetical path order, one worker, one shared database.** So a spec that
  *writes* shared state is only ever seen by the files sorting after it — `npx playwright test --list`
  prints the order, and a **rename moves a spec in that order** (batch 27's `portions.spec.ts` →
  `allowance.spec.ts` moved it from the middle to second). Anything a spec asserts as an **absolute
  count of a shared thing** is therefore a landmine: `settings.spec.ts` counted its version history
  as `3` and `4` and read the seeded version as `nth(2)`. It now takes a baseline with
  `versionCount(page)` and asserts `before + 1`, and reads the oldest version as `.last()` — the same
  claim, stated so that it does not depend on who ran first.
- **Two consecutive saves on the settings screen need a `page.reload()` between them in e2e.** A
  successful save *clears* the form, and the revalidated render that does it can land **after** the
  next `fill`, rewinding the field to the stored value; the second save then appends a version that
  changed nothing and every history index below it shifts. `selectOption` happens to survive the
  race, `fill` does not — reload and re-assert the stored value before typing again.
- **The customer record renders two different household blocks.** `/kunden/<active>` gives the live
  editor (`household-editor.tsx`), `/kunden/<archived>` gives `HouseholdReadOnly` in `[id]/page.tsx`.
  Both hold the same tiles, so a screen-level change has to be checked on one of each — **seed
  customer 6 is archived**.
- **Counts baked into prose go stale in files no story names, in both directions.** Removing two
  settings made "the ten fields of the settings form", "nine of the ten need no translation",
  "Seven of the nine settings", "printed only five of the eight settings" and a `toHaveLength(8)` all
  wrong at once; *adding* a field or a tile does the same. After any such change,
  `grep -rn "nine\|ten fields\|eight\|six tiles\|four figures" src/` — the suite catches only the
  assertion. The same applies to an **exported type you delete or rename**: `grep -rn "<TypeName>"`
  finds the `{@link}`-shaped references left behind in comments, which no gate sees.
- **Prettier does not reflow comments.** Shortening a sentence inside a `/** */` block leaves a
  ragged short line that `prettier --check` happily accepts. Re-wrap the paragraph by hand.
- **Prettier owns every markdown table in `docs/`, and `npm run lint` does not check it.** `lint` is
  `eslint` alone; markdown formatting is `npm run format:check` (the pre-commit hook runs it). Widen
  one cell and Prettier re-flows every row of that table, so run `npx prettier --write` on the files
  you touched *before* reading the diff — otherwise you are reviewing a re-alignment.
- **Documentation has no gate.** Nothing fails CI when a chapter goes stale, so a change that adds or
  removes a concept finishes with an explicit sweep — `grep -rin <concept> --include="*.md" .` — and
  the answer is not always zero: `docs/architecture/01`'s non-goals and `09`'s „Decisions that were
  reversed“ exist precisely to keep a withdrawn idea named, with its reason. **`docs/architecture/`
  is the tree to update**; `docs/technical_documentation.md`, `tech_stack_architecture_sketch.md` and
  `fd_dev_setup_overview.md` were retired in #102 and only the archived progress logs still name them.
- **`docs/archiv/` is annotated, never rewritten** (root `CLAUDE.md`). A dated `> **Note (YYYY-MM-DD).**`
  blockquote goes after the *block* carrying the claim, not inside the table cell or list item that
  states it, and names which bullets it answers when it follows several. The per-story PRDs in
  `tasks/` are the same kind of record: they say what was asked for, so a superseded one gets a
  banner and the rest are left alone.
- **`playwright-cli` needs a role locator, not a testid, to fill the counter's Nummer input**, and a
  `cmd-a || cmd-b` shell chain hides a silent no-op — the first command exits 0 without filling
  anything. Run the fill on its own and re-snapshot to confirm it landed. Verify against
  `npm run start` (production build), not `next dev`.
- **WebKit's nav-bar click is flaky at roughly one failure in two full-suite runs**
  (`navigation.spec.ts` „von Kunden verwalten aus …“, `waitForURL` timing out after the click). It
  passes alone and on re-run. If it recurs, the click needs the „wait for React to own the control“
  treatment `day.ts`'s `hydrated` gives the fields (#116) — worth watching rather than chasing.
- **Prior runs' patterns are in the archive**, not in this file:
  `scripts/ralph/archive/2026-08-24-us-27-remove-portion-allowance/progress.txt` is the most recent
  (a deletion, run outside-in), `archive/2026-08-07-us-26-price-cap/` before it, and
  `archive/2026-08-07-us-25-globally-unique-card-numbers/progress.txt` keeps a long consolidated list
  (playwright-cli usage, e2e customer-number bands, killing the dev server safely, testid greps).
  Read the most recent two before any UI or e2e story.

### Where batch 28 starts
Batch 27 (the portion allowance) is **merged into `main`** — PR #120, squashed as `84c6c8e`. So the
tile slot on the counter's four-track figures row is genuinely free, the „Mengen und Preise“ card is
already re-spanned 3+3+3+3, and `Allowance` is `{ grownUps, children, priceCents }` with no third
derived figure. Nothing in this batch should have to remove a portion: `grep -ri portion src/ tests/
prisma/` returns only an absence-assertion regex in `tests/e2e/allowance.spec.ts` and one PRD path
citation in a comment, both deliberate.

Two hazards this batch carries, both stated in `prd.json` and worth reading twice:
- Its `SettingsChange` variant is **the only one without `from`/`to`** — a list's change is a set of
  row changes, and printing two whole rules side by side is the restatement the history was
  rewritten to stop doing.
- Between story 2 and story 6 the settings action passes an **empty rule**, so saving the form on
  that branch erases the rule mid-batch. That is why story 6 is in this batch and not a follow-up —
  do not leave the tree in that state.
---

## 2026-08-24 — US-001 (The egg rule and the count it yields, domain)
- New pure module `src/domain/policy/eggs.ts`: `EggRuleRow`, `EggRule`, `createEggRule` (sorts, then
  validates the staircase) and `eggsFor` (highest threshold reached, else 0). Strict TDD — the whole
  test file was written and run red before a line of the module existed.
- Two new domain errors, `DuplicateEggThreshold` and `EggsNotIncreasing`, both tiered `refusal`.
- Files: `src/domain/policy/eggs.ts` (new), `src/domain/policy/eggs.test.ts` (new),
  `src/domain/policy/require-integer.ts` (new, see below), `src/domain/policy/settings.ts`,
  `src/domain/errors.ts`, `src/app/notice-tier.ts`.
- Gates: lint, typecheck, `test:coverage` (50 files, 1108 tests, domain+application 100%) and
  `build` all green.
- **Learnings for future iterations:**
  - **`requireInteger` now lives in `src/domain/policy/require-integer.ts`**, not in `settings.ts`.
    It had to move before story 1 could reuse it: story 2 puts `eggRule` on `Settings`, so
    `settings.ts` imports `eggs.ts`, and leaving the helper where it was would have made the two
    modules import each other. Landed as its own `refactor(domain):` commit before the feature.
  - **The `notice-tier.ts` header counts the codes and went stale on its own**: it claimed a *33rd*
    code would fail the build when there were already 33. Recount rather than increment —
    `python3 -c` over the `DomainErrorCode` union is two lines — and it now says 36th for 35 codes.
  - **A `try`/`catch` with an `instanceof` narrow is how these tests read an error's fields**, since
    `expect(fn).toThrow(new SomeError(...))` only compares the message. `toThrow(new InvalidSettings(
    field, requirement))` *is* worth using where the whole point is the exact field name and
    requirement sentence — it asserts both in one line.
  - Deriving from a sorted list needs no `findLast`: `eggsFor` walks the rule and keeps overwriting,
    so the last row the household reaches is the answer. One branch, one loop, 100% covered.
---

## 2026-08-24 — US-002 (The rule becomes a setting, and a change to it is named, domain)
- `Settings.eggRule` built through `createEggRule` in `createSettings`, `SettingsInput` taking the
  unsorted `ReadonlyArray<EggRuleRow>`, `"eggRule"` last in `SETTINGS_FIELDS`, and an `eggRule` case
  in `isUnchanged` that asks `diffEggRule` rather than comparing two array references.
- `diffEggRule` + `EggRuleRowChange` in `eggs.ts` (matched by threshold, reported ascending), the
  `{ field: "eggRule"; rows }` variant of `SettingsChange`, and `diffSettings` emitting it only when
  a row moved.
- `describeChange` in the settings page renders it through `de.settings.history.rowChanges` and one
  dictionary function per kind; `de.settings.fields.eggRule = "Eierregel"`.
- Every `createSettings` call site updated: 13 test builders with `eggRule: []`, and the repository's
  `toDomain`, the seed and the settings action with an empty rule and a comment naming the story
  that replaces it (US-004, US-004, US-006).
- Files: `src/domain/policy/{eggs,settings,settings-diff}.ts` + their tests, `src/i18n/de.ts`,
  `src/app/einstellungen/{page.tsx,actions.ts}`, `src/infrastructure/prisma/{seed,settings-repository}.ts`,
  11 further application/domain test builders.
- Gates: lint, typecheck, `test:coverage` (50 files, 1133 tests, domain+application 100%) and
  `build` all green.
- **Learnings for future iterations:**
  - **The Änderungsverlauf's rows are ascending, not the PRD example's order.** `prd.json`'s
    illustrative string reads „ab 8 … · ab 5 … · ab 3 …“ (descending), but FR-6 says a rule "must
    always be displayed in that order" — the ascending order `createEggRule` sorts to. Ascending is
    what `diffEggRule` returns and what US-007's `FullValues` should match; the example is phrasing,
    not order.
  - **`eggRule` is the third `SETTINGS_FIELDS` entry needing a case in `isUnchanged`.** The default
    `previous[field] === next[field]` works only for primitives; `weekAnchor` was the first
    exception and any future object- or list-valued setting is the next one.
  - **The audit's changed-field count is asserted as a literal** in
    `src/application/settings/settings.test.ts` ("treats every field as changed when there is no
    previous version"). It moved 6 → 7. Any story adding or removing a settings field must re-point
    it and its comment, which recounts the history of the number.
  - **A regex insert after `priceCap` hits the *parameter list* too.** `settings-repository.test.ts`
    builds its version with `version(recordedAt, quotaN = 240, priceCap = null)`, so the first
    `priceCap,` in the file is a parameter, not an object key. Typecheck caught it; check the hunk.
  - **German plurals for the rule live in two module-level helpers in `de.ts`** (`fromPersons`,
    `eggCount`), above the dictionary, because „ab 1 Person“ and „1 Ei“ are both reachable
    configurations and three phrasings share the grammar. First helpers of their kind in that file.
---

## 2026-08-24 — US-003 (The allowance seam states the egg count, application)
- `Allowance` gains `readonly eggs: number`, derived in `allowanceAt` as
  `eggsFor(settings.eggRule, grownUps + children)` — the composition's own total, never a second
  count of the members array. `CounterCustomerView` copies it; `read-customer.ts`, `read-card.ts`
  and `list-customers.ts` needed no change at all.
- Files: `src/application/allowance/describe-allowance.ts` + test,
  `src/application/customers/lookup-customer.ts` + test, `docs/architecture/06-runtime-view.md`.
- Gates: lint, typecheck, `test:coverage` (50 files, 1139 tests, domain+application 100%) and
  `build` all green. No UI in this story, so no browser check.
- **Learnings for future iterations:**
  - **Typecheck is the census of `Allowance` constructors.** Adding a required field to `Allowance`
    and to `CounterCustomerView` broke *nothing* — no fixture, fake or app route builds either shape
    by hand, they all read what the seam returns. That is the seam working, and it means a future
    field on either type is a one-file change plus its tests. Contrast `Settings` (batch 28's
    US-002 notes), where the same move touched 20 call sites.
  - **`describeAllowances` had no test file of its own** before this story — its coverage came
    entirely through `list-customers.test.ts`. The new "one read of the settings history" test adds
    a `reads` counter to the local `FakeSettingsRepository`; that counter is the only way to assert
    the no-query-per-row promise, since the result alone cannot show how often the port was called.
  - **The two exhaustive `toEqual` assertions in `describe-allowance.test.ts` are the tripwire** for
    a new derived figure: they compare the whole object, so a field added to `Allowance` fails them
    until it is stated. Keep them exhaustive — the `toMatchObject` cases elsewhere would not notice.
  - **`docs/architecture/06-runtime-view.md` names the seam's return value** in the counter sequence
    diagram (`Allow-->>Lookup: counts, price (capped), eggs`). Any story that adds or removes a
    derived figure makes that line wrong; the docs story at the end of a batch is too late, so fix
    it in the same commit.
---

## 2026-08-25 — US-004 (The rule is stored and versioned, infrastructure)
- New `EggAllowanceRow` model: a child table of `SettingsVersion` with `minPersons`, `eggs`,
  `onDelete: Restrict` spelled out, `@@unique([settingsVersionId, minPersons])` and
  `@@index([settingsVersionId])`. Its `///` comment carries the argument for rows over a JSON column
  or three fixed columns.
- Migration history regenerated as `20260825000027_init` (pre-release rules), the hand-written
  `Customer_customerNumber_onRegister_key` partial index re-appended, `npm run db:reset` run.
- `PrismaSettingsRepository.append` writes the rows as a **nested create** inside
  `settingsVersion.create`; `listVersions` includes them ordered by `minPersons` and `toDomain`
  passes them through `createSettings`.
- The seed carries DF's real rule (3 → 6, 5 → 12, 8 → 18) and its comment says that this is the one
  seeded value that is *not* provisional. US-002's temporary empty rule is gone from the repository
  and the seed.
- Files: `prisma/schema.prisma`, `prisma/migrations/20260825000027_init/migration.sql` (replacing
  `20260824183935_init`), `src/infrastructure/prisma/{settings-repository,seed,test-support}.ts`,
  `src/infrastructure/prisma/{settings-repository,schema}.test.ts`,
  `docs/architecture/08-crosscutting-concepts.md`.
- Gates: lint, typecheck, `test:coverage` (50 files, 1146 tests, domain+application 100%), `build`
  and `format:check` all green. `/einstellungen` checked against the reset database on
  `npm run start`: 240, 5,00 € and „Maximalpreis“ render, no „Noch nicht konfiguriert“.
- **Learnings for future iterations:**
  - **`clearRegister` was the wrong place, and the PRD's hint to extend it is a trap.** The egg rows
    hang off `SettingsVersion`, not off `Customer`, so the register helper never touches them. What
    broke was `settings-repository.test.ts`'s `beforeEach`: `settingsVersion.deleteMany()` is a
    foreign-key error the moment a version carries a rule, and every version does now that the seed
    does. There is a `clearSettings(prisma)` sibling in `test-support.ts` for it — **use it rather
    than deleting the two tables inline**, and extend it when the next child table of a version
    arrives.
  - **`tail -N old.sql >> new.sql` re-appended one line too many.** The hand-written index block is
    the last 15 lines of the old migration, not 17; the extra two duplicated
    `CREATE INDEX "AuditEntry_when_idx"`. SQLite would have failed the reset on it, but the reset ran
    *before* the check — always finish with `diff /tmp/old.sql prisma/migrations/*_init/migration.sql`
    and read the whole diff, not just that the new table is in it.
  - **Verify the regenerated database by asking `sqlite_master` for the index names**, from a script
    at the repo root: `npx tsx /tmp/x.ts` fails with `Cannot find module '@prisma/client'` because
    tsx resolves from the *script's* directory, and `npx tsx -e` cannot take `$queryRaw` (the shell
    eats the `$`, esbuild then reports `Syntax error "$"`). Write the file into the repo, run it,
    delete it. Top-level `await` is out too — tsx transforms to CJS here, so wrap it in `main()`.
  - **A required Prisma relation defaults to `Restrict` already**, so writing it out changes nothing
    the database does — it changes what the file says, and `schema.test.ts` now asserts the words are
    there. Note that **most existing relations do *not* spell it out** (`HouseholdMember`,
    `Certificate`, `Card`, `DistributionRecord`, `ReminderLog` all rely on the default), so a
    general "every relation states its action" test fails today. If that is wanted it is a
    deliberate refactor of its own, not a line in a feature story.
  - **Chapter 8's „Nine tables“ was made wrong by this commit**, so it and the ER diagram were fixed
    here rather than left to the batch's documentation story (US-009). The rest of that story's
    sweep — the configuration-as-data prose, chapter 7's seed row, `tasks/README.md`'s provisional
    table, the ADR — is untouched and still owed.
  - **The last `eggRule: []` placeholder is `src/app/einstellungen/actions.ts:235`**, which US-006
    replaces. Until then, saving the settings form erases the rule — the hazard the batch header
    warns about is still live.
---

## 2026-08-25 — US-005 (The counter, the record and the live preview state the eggs, presentation)
- An „Eier“ `Stat` in the **third** slot of the four-track figures row on all three screens, ahead of
  the price: `counter-eggs` on the counter, `eggs` in `HouseholdReadOnly` and `eggs` in the live
  editor. The customer list and the card view were not touched.
- `AllowanceValues = PriceValues & Pick<Settings, "eggRule">` in `settings.ts` is what carries the
  rule to the browser; the editor's `derived()` calls `eggsFor(policy.eggRule, grownUps + children)`
  and re-implements no lookup. `priceFor` keeps the narrower `PriceValues`.
- `de.customers.derived.eggs = "Eier"`, and `de.customers.record.householdHint` now names all four
  figures.
- Files: `src/app/ausgabe/counter-lookup.tsx`, `src/app/kunden/[id]/{page.tsx,household-editor.tsx}`,
  `src/domain/policy/settings.ts`, `src/i18n/de.ts`.
- Gates: lint, typecheck, `test:coverage` (50 files, 1146 tests), `build` and `format:check` green.
- Browser-checked against `npm run start` with two hand-seeded households (901: two grown-ups and a
  child; 902: two grown-ups, archived afterwards for the read-only block). Accessibility snapshots
  read „Eier 6“, „Eier 12“ and „Eier 0“ as one `<p>` each; the editor moved 6 → 6 → 12 as a fourth
  and then a fifth member was added, and emptying the household turned it to „—“ beside the counts'
  own „—“.
- **Learnings for future iterations:**
  - **The PRD names `AllowanceValues` as if it already existed** — the type on `main` is
    `PriceValues`. It was **added beside** `PriceValues` rather than renamed over it: `priceFor`
    still takes only the three price values, and widening its parameter would have said the eggs
    were part of the sum and forced an `eggRule` into every price test. A PRD naming a type it
    expects to find is a design hint, not a fact about the tree.
  - **`de.customers.record.householdHint` enumerates the derived figures** („Erwachsene, Kinder und
    Preis …“) and no gate sees it. It now says „aus dem Haushalt“ rather than „aus den
    Geburtsdaten“, because the eggs follow heads and not ages. Any story adding or removing a tile
    on that screen has to re-read that sentence — `de.customers.new.intro` is the same shape for the
    registration form.
  - **`playwright-cli fill` on a `DateInput` works through `getByTestId`** — the date fields take a
    testid per row (`member-birth-date-<n>`), unlike the counter's Nummer input, which still needs a
    role locator. `add-member` and `remove-member-<n>` are testids too, so the whole editor can be
    driven without a snapshot between steps.
  - **Seeding a household by hand for a browser check needs two fields the e2e specs' snippets do
    not show you**: `Certificate.recordedAt` is required, and the archive pair is
    `archiveReason` + `archivedAt` (not `archivedReason`). Write the script into the repo root, run
    `npx tsx ./x.ts`, delete it — and finish with `npm run db:reset` so the next iteration does not
    inherit customers 901/902.
  - **Still owed to US-009:** `docs/architecture/05-building-block-view.md:73` says the `allowance/`
    seam turns a household plus a date "into the counts and the price". US-003 made that wrong; the
    runtime view was fixed then and this cell was not.
---
