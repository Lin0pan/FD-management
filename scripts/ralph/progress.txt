# Ralph Progress Log
Started: Fri Aug  7 2026 (batch 26 — US-26 price cap)

## Codebase Patterns
- **Adding a `DomainErrorCode` is a three-file change**: the union in `src/domain/errors.ts`, the
  class beside it, and `TIERS` in `src/app/notice-tier.ts`. `TIERS` is a `Record` over the union, so
  the *build* fails until the code is tiered — that is the mechanism, not an obstacle. The header
  comment of `notice-tier.ts` counts the codes ("a 32nd code…", "the six red ones"), so it goes
  stale silently; update it with the entry.
- **The same mechanism guards `SettingsChange`**: its `switch` in `src/app/einstellungen/page.tsx`
  is exhaustive with no `default`, so adding a member to the union fails the build until the screen
  renders it. Do not silence it with a `default` branch.
- **An error class with no caller yet still needs a test**, because `domain/` coverage is gated at
  100%. `src/domain/errors.test.ts` is where those live — see the `MissingAuditReason` block for the
  shape, including the comment saying which story will call it.
- **`errors.ts` cannot import from `domain/card/`**: `cardNumber.ts` imports `errors.ts`, so a
  message that wants to read `66k1` writes the `k` inline rather than calling `formatCardNumber`.
- **`docs/technical_documentation.md` explains each domain module in a `### <path>` section** and
  lists the error classes by name in the `src/domain/errors.ts` section. Adding an export or an
  error makes both stale and no gate can tell — grep the file for the module path *and* for the
  vocabulary of the rule being changed.
- **Adding a field to `Settings` is a wide change before it is a deep one.** `createSettings` is
  called from the repository, the seed, the settings server action and every hand-written fake in
  the application tests; all of them must supply the new field before anything compiles. Land the
  field with a placeholder value at each call site, then replace them in the story that owns each.
- **A Prisma `P2002` cannot always name *which* constraint was broken.** `meta` is
  `{ modelName, target: [columns] }`, but where two unique indexes both cover the offending row
  SQLite reports only one of them, and not the one that was declared or created first. Where the
  distinction matters, match the target for the *shape* and then ask the database the question that
  actually distinguishes the two faults (`card-repository.ts`'s `issue` counts the customer's own
  rows on that index). For a **nested** write Prisma reports the *rooted* model — a card refused
  inside `customer.create` comes back as `modelName: "Customer"` — so the model name alone tells
  nothing about which table failed.
- **Regenerating `prisma/migrations/` drops the hand-written partial unique index** on
  `Customer.customerNumber` (`Customer_customerNumber_onRegister_key`) at the end of the migration
  SQL. `src/infrastructure/prisma/schema.test.ts` greps for it and for the `onDelete: Restrict`
  clauses, so a lost line fails CI rather than production — but re-add it deliberately, and run
  `npm run db:reset` afterwards or the schema and the database drift silently.
- **The e2e customer-number bands, as they now stand.** Below the quota of 240: counter 201–206 and
  239, portions 211, serve 221–222, reminders 231, registration 232–234, card numbers 236. Above it:
  block 241, reissue 251, age-13 271, customer-list 281–285, customer-record 291, group-progress
  301–303, group-walk 311–315. A spec whose number must be **pickable in the /kunden/neu dropdown**
  has to sit below 240 — the pool is `1..quotaN` — and everything else should stay above it, clear of
  the low sequence the allocating specs (registration, card, archive, reregistration) consume.
- **Get the *files* out of a wide type break, not the errors**:
  `npm run typecheck 2>&1 | grep -E "^src/" | cut -d'(' -f1 | sort -u`. Adding a required field to
  `Settings` breaks ~18 files at once and the raw output is a screen per error.
- **Prior runs' patterns are in the archive**, not in this file:
  `scripts/ralph/archive/2026-08-07-us-25-globally-unique-card-numbers/progress.txt` is the most
  recent and keeps a long consolidated list (playwright-cli usage, e2e customer-number bands,
  killing the dev server safely, testid greps). Read it before any UI or e2e story.
---

## Fri Aug 7 2026 — US-001 (The cap exists and priceFor honours it)
- `Settings` gained `readonly priceCap: Cents | null` after `pricePerChild`, documented with why
  it is nullable rather than `0`-flagged. `createSettings` passes `null` through unconditionally
  and sends a number to the existing `requireInteger("priceCap", …, 0)`; no second helper, no
  cross-field rule against `pricePerGrownUp`.
- `PriceValues` widened to `Pick<Settings, "pricePerGrownUp" | "pricePerChild" | "priceCap">`, and
  `priceFor` now returns `Math.min(perHead, priceCap)` when a cap is set. `portionsFor` untouched —
  the cap caps money, not food.
- 16 new named tests written first, red before green (10 failing on the first run): the six
  validation rules and the seven pricing rules the PRD names, including FD's own case
  (4 grown-ups + 3 children at 200/100 with a cap of 500 → 500, not 1100).
- Files: `src/domain/policy/settings.ts`, `src/domain/policy/settings.test.ts`,
  `src/app/kunden/[id]/page.tsx` (the `AllowanceValues` object gains the cap),
  plus the `priceCap: null` placeholder at `src/infrastructure/prisma/settings-repository.ts`,
  `src/infrastructure/prisma/seed.ts` and `src/app/einstellungen/actions.ts`, and the settings
  baseline in 12 test files.
- Gates: lint, typecheck, `test:coverage` (1061 tests, domain+application 100%) and `build` green.
- **Learnings for future iterations:**
  - The "wide before deep" pattern held exactly as the Codebase Patterns entry predicts: **18 files
    failed typecheck** the moment the field became required on `SettingsInput`. Fifteen of them were
    a one-line insert after `pricePerChild: 100,` in a `settingsInput` baseline — a scripted insert
    keyed on that exact line hit every one and nothing else, because no other literal in the repo
    spells that line.
  - `npm run typecheck 2>&1 | grep -E "^src/" | cut -d'(' -f1 | sort -u` is the fastest way to get
    the *list of files* to fix out of a wide type break; the raw output is one screen per error.
  - Each temporary placeholder carries a `// FOR NOW — US-26.x …` comment naming the story that
    removes it, so a later iteration greps `FOR NOW` rather than re-reading this log.
  - The `AllowanceValues` comment in `src/app/kunden/[id]/page.tsx` counts its fields out loud
    ("the four per-head values"), so widening `PriceValues` makes it stale with nothing to catch it.
    It now says five, and the cap is not per head.
---

## Fri Aug 7 2026 — US-002 (A changed cap is named in the history and the audit entry)
- `SETTINGS_FIELDS` gained `"priceCap"` after `"pricePerChild"`, so `changedSettingsFields` reports
  it and `updateSettings` writes it into the audit entry. `isUnchanged` needed no special case:
  `previous[field] === next[field]` compares `null` to `null` correctly.
- `SettingsChange` gained `{ field: "priceCap"; from: Cents | null; to: Cents | null }` and
  `diffSettings` compares the whole of `Cents | null`, so *introduced* (`null` → value), *removed*
  (value → `null`) and *changed* all report, while `null` → `null` does not.
- `describeChange` in `src/app/einstellungen/page.tsx` gained the case through a `describeCap`
  helper; no `default` branch was added. `de.settings.prices.noCap` = „kein Maximalpreis“ and
  `de.settings.fields.priceCap` = „Maximalpreis je Ausgabe“, so a row reads
  `Maximalpreis je Ausgabe: kein Maximalpreis → 5,00 €`.
- 9 tests written first, red before green (8 failed on the first run): four transitions on each of
  the two functions plus the two full-field-order lists.
- Files: `src/domain/policy/settings.ts`, `settings-diff.ts` and both their tests,
  `src/app/einstellungen/page.tsx`, `src/i18n/de.ts`, `src/application/settings/settings.test.ts`.
- Gates: lint, typecheck, `test:coverage` (1070 tests, domain+application 100%) and `build` green.
- **Learnings for future iterations:**
  - **Widening `SETTINGS_FIELDS` breaks a *count* assertion, not a type.** `updateSettings`'s
    "treats every field as changed when there is no previous version" test in
    `src/application/settings/settings.test.ts` asserts `toHaveLength(7)`. It is now 8, moved
    deliberately with a comment saying why — grep the application tests for `toHaveLength` before
    assuming a field addition is type-safe end to end.
  - **`formatEuros(0)` is `0,00 €`, which is why every read-back of the cap needs its own null
    branch before formatting.** `describeCap` in `einstellungen/page.tsx` is that branch for the
    history; US-004 needs the same shape again for the form value and the version summary line, and
    losing it turns *no cap* into *free for everyone* on the next save.
  - The exhaustive-`switch` guard worked as the Codebase Patterns entry promises: the build failed
    on `page.tsx` the moment the union member existed, before any test was written for the screen.
  - `de.settings.errorFields.priceCap` is deliberately **not** added yet — US-004 owns it together
    with the form field it marks. `fields.priceCap` had to land here because the history row names
    the field.
  - No browser verification in this story: nothing can set a cap yet (the repository still writes
    `priceCap: null` until US-003 and the form has no control until US-004), so the new history row
    is unreachable on screen. US-004 verifies it end to end, as its criteria say.
---
