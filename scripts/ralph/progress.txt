# Ralph Progress Log
Started: Thu Jul 23 20:41:04 CEST 2026
---

## Codebase Patterns
- Domain policy modules live in `src/domain/policy/`. Per-head derivations (price, portions) are
  pure functions that take the counts and the settings values as parameters — NO baked-in defaults,
  because policy values are configuration (CLAUDE.md). Reuse `HouseholdComposition` ({grownUps,
  children}) from `src/domain/customer/householdComposition.ts` and `Pick<Settings, ...>` for the
  settings-derived params rather than redeclaring `{grownUps, children}` shapes.
- `priceFor(settings, grownUps, children)` already lives in `src/domain/policy/settings.ts` (per-head,
  returns integer cents) — US-002 is effectively implemented and tested on main; just needs marking.
- Coverage is gated at 100% on domain/ + application/ (`vitest.config.ts`). TDD: write the
  invariant-named test first. Test files sit beside the module (`foo.ts` + `foo.test.ts`).
- Quality gate command: `npm run lint && npm run typecheck && npm run test:coverage && npm run build`.
---

## 2026-07-23 20:44 - US-002 (PricePolicy)
- Verified `priceFor(settings, grownUps, children)` in `src/domain/policy/settings.ts` (already on
  branch): derives `grownUps*pricePerGrownUp + children*pricePerChild` as integer cents. Per-head
  derivation, so it never interpolates/extrapolates and every household size is priceable (empty=0).
- No code change needed. Tests in `settings.test.ts` cover exact per-head, single-person, arbitrary
  size, empty household, and a valid zero-cent price. Flipped `passes: true`.
- All gates green: lint, typecheck, 100% coverage, build.
- **Learnings for future iterations:**
  - US-002's AC talks of an "exact (grownUps, children) row" / "no match" — that is legacy
    table-lookup wording. The domain uses per-head derivation, which subsumes it: no row table to
    keep in step, so "no match" simply can't occur. Don't reintroduce a lookup table.
  - Only US-003 (`describeAllowance`, application layer) remains. Build an app helper that resolves
    settings at a date (settings repo + fake clock), derives counts from birthdates
    (`householdComposition`), then combines `portionsFor` and `priceFor`. Follow the fake-repo +
    fake-clock pattern in `src/application/settings/` and `distribution/get-week-colour.ts`.
---

## 2026-07-23 20:42 - US-001 (PortionPolicy)
- Implemented `portionsFor({grownUps, children}, {portionsPerGrownUp, portionsPerChild})` in
  `src/domain/policy/portions.ts`, deriving portions per head with no baked-in defaults.
- Files changed: src/domain/policy/portions.ts (new), src/domain/policy/portions.test.ts (new).
- Tests cover single-person household, household with children, zero children, and portionsPerChild=0.
- All gates green: lint, typecheck, 335 tests, 100% coverage, build.
- **Learnings for future iterations:**
  - US-002 (`priceFor`) is ALREADY implemented + tested in settings.ts (lines ~202, tests in
    settings.test.ts) — next iteration only needs to verify and flip `passes: true`, no code.
  - US-003 (`describeAllowance`, application) still needs building: an app helper resolving settings
    at a date via the settings repo/clock and returning {grownUps, children, portions, priceCents}.
    Look at `src/application/settings/read-current-settings.ts` and `distribution/get-week-colour.ts`
    for the fake-repo + fake-clock test pattern.
---
