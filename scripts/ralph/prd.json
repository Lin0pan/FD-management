{
  "project": "FD-Management",
  "branchName": "ralph/us-13-age-13-reclassification",
  "description": "US-13 Reclassify Children Automatically at Age 13 — Counts, portions and price follow a 13th birthday with no staff action; affected customers appear on a cards-due-for-reissue list. A stale card is never grounds to turn anyone away.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Boundary-exhaustive tests for the 13th-birthday split",
      "description": "As a developer, I need the 13th-birthday boundary pinned down by tests, because every downstream number depends on it. Source: tasks/prd-us-13-age-13-reclassification.md §US-13.1",
      "acceptanceCriteria": [
        "Extends the existing householdComposition tests — no new production rule is added",
        "Test: born exactly 13 years ago today counts as a grown-up",
        "Test: born 13 years ago tomorrow counts as a child",
        "Test: born 13 years ago yesterday counts as a grown-up",
        "Test: a 29 February birthdate in a non-leap year becomes a grown-up on 1 March, with the convention documented",
        "Test: a 29 February birthdate in a leap year becomes a grown-up on 29 February",
        "Test: evaluated at 23:59 and 00:01 Europe/Berlin around the birthday, the answer flips exactly once, at local midnight",
        "Every test drives an explicit today parameter",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": "The boundary tests exposed a defect rather than only confirming the rule: composition and ageInYears read `today` as its UTC day, so the flip happened at 01:00/02:00 Berlin. `today` is now read through berlinDayKey; the birthdate keeps its UTC day (it is a stored calendar day). No change to the 13-year threshold or the § 188 Abs. 3 BGB roll-over. The 13th birthday of a 29 February child can never fall on 29 February (leap year + 13 is never a leap year), so the leap-year anniversary is pinned on ageInYears at the 16th birthday instead."
    },
    {
      "id": "US-002",
      "title": "Card count snapshot fields",
      "description": "As a developer, I need the counts printed on a card recorded at issue time, so the app can tell which physical cards have gone stale. Source: tasks/prd-us-13-age-13-reclassification.md §US-13.3",
      "acceptanceCriteria": [
        "Card.grownUpsAtIssue Int and Card.childrenAtIssue Int added, written by issueCard from the composition derived at issue time",
        "A schema.prisma comment states these are a snapshot of the printed card, not household truth, so a future maintainer does not remove the duplication",
        "Integration test seeds a card issued with 1/1 counts and a member who has since turned 13, and asserts the difference is detectable",
        "Migration committed under prisma/migrations/",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Modelled as `IssuedCard.countsAtIssue: HouseholdComposition` in the domain, mapped to the two flat Int columns in the adapter — the comparison US-003 needs is then one value-object equality rather than four field reads. Written by issueCard AND by the registration's first card (customers.create), because a first card and a replacement are the same object. The init migration was regenerated rather than corrected: the columns are NOT NULL with no honest default, so a stacked ALTER would describe a system no one ever ran; the hand-written partial unique index was re-appended. Required-field ripple hit 9 test files plus 6 e2e specs."
    },
    {
      "id": "US-003",
      "title": "listCardsDueForReissue query",
      "description": "As a staff member, I want a list of customers whose card shows counts that no longer match their household. Source: tasks/prd-us-13-age-13-reclassification.md §US-13.2",
      "acceptanceCriteria": [
        "listCardsDueForReissue(deps) returns active customers whose current card's recorded counts differ from the counts derived today",
        "Each result carries customer number, name, card number, counts on the card, counts today, and a reason of AGE_13 when a birthday explains the difference or HOUSEHOLD_CHANGE otherwise",
        "Archived and blocked customers are excluded, and the exclusion is documented",
        "Test with a fake clock proves a household where a child turns 13 between two clock values appears on the list with no write having occurred in between",
        "Tested against hand-written fakes and a fake clock",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": true,
      "notes": "The comparison itself is a domain rule (`staleCountsReason` in domain/card/staleCounts.ts): AGE_13 only when the household is the same size AND grown-ups went up — a birthday is the whole explanation or it is not named as one. The use case is `listCardsDueForReissue` (application/customers/cards-due-for-reissue.ts) over one new port method, `CustomerRepository.listWithStatus(status)`, which the use case calls with \"ACTIVE\" so the blocked/archived exclusion is stated where its reason lives rather than hidden in the adapter. The register is read whole and compared in application code because one side of the comparison is a rule over birthdates — not expressible as a WHERE clause; documented in the use case, the port and the adapter. Port ripple hit 4 fake repositories."
    },
    {
      "id": "US-004",
      "title": "Cards-due-for-reissue screen",
      "description": "As a staff member, I want to see which cards need reissuing, without being nagged — a stale card is never grounds to turn anyone away. Source: tasks/prd-us-13-age-13-reclassification.md §US-13.4",
      "acceptanceCriteria": [
        "Route /karten-neuausstellung lists affected customers with both count sets side by side and the reason",
        "The page states in German that this is not urgent and that a stale card is never grounds to turn anyone away",
        "Each row has a 'Karte neu ausstellen' action using reason STALE_COUNTS; on success the row disappears",
        "The counter screen shows a low-key note for a stale card that does not look like a warning or a verdict and never suppresses the serve action",
        "A count badge for the list is shown on the home screen",
        "All user-facing strings come from src/i18n/de.ts",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 4,
      "passes": true,
      "notes": "Route /karten-neuausstellung: page.tsx (list) + stale-card-controls.tsx (client) + actions.ts (reason fixed to STALE_COUNTS, never read off the form — the same argument the loss control makes) + reissue-state.ts. The row names the NEXT card number, which is new on the read model (`CardDueForReissue.nextCardNumber`), because a successful reissue removes the row and takes any success message with it — the number staff copy onto the card has to be shown before the write. The counter note needed two new fields on `CounterCustomerView`: `countsOnCard` (the card's snapshot, for the note to name) and `staleCounts` (the rule's answer, compared against the counts the lookup already derived, so note and numbers cannot disagree). The home badge goes through `countCardsDueForReissue`, which is `listCardsDueForReissue(...).length` — badge and screen are then one statement — and both `/` and the list are `force-dynamic`, since the answer moves at midnight with nothing written. Verified in a real browser against a scratch DB with the clock pinned past a 13th birthday: badge 1 → row with 1/1 vs 2/0 and \"13. Geburtstag\" → counter shows the grey note with the serve button still there → reissue → row gone, badge 0, card 50k2, note gone."
    },
    {
      "id": "US-005",
      "title": "E2E: a birthday changes the numbers with no user action",
      "description": "As a developer, I need a Playwright spec proving the age-13 reclassification is automatic. Source: tasks/prd-us-13-age-13-reclassification.md §US-13.5",
      "acceptanceCriteria": [
        "Spec with a controllable clock asserts a customer with a 12-year-old member shows counts 1/1 with a given portion allowance and price",
        "Spec advances the clock past the member's 13th birthday, reloads, and asserts counts are 2/0 with updated portions and price — with no user action in between",
        "Spec asserts the customer now appears on the cards-due list, is still servable at the counter, and disappears from the list after a reissue",
        "npm run test:e2e passes",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": true,
      "notes": "tests/e2e/age-13.spec.ts, customer number 271, child born 2013-01-15. The clock moves between two RED distribution Thursdays — 08.01.2026 (child 12) and 22.01.2026 (child 13) — so the group, the calendar and the certificate are all held still and only the age moves; both days follow from the seeded anchor 2026-W02=RED, weekday 4. \"No user action in between\" is asserted as an absence: one Prisma snapshot of the household's row, members, cards, distribution records and the audit-entry count, taken either side of the clock change and compared, so the four figures on the record cannot have come from a write. The home badge counts the whole shared register, so it is only ever compared with itself (before + 1, then back), and the row is addressed by data-customer-number rather than by index — the neighbouring specs leave stale households behind (portions.spec adds a member after its card was printed)."
    }
  ]
}
