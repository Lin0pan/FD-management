{
  "project": "FD-Management",
  "branchName": "ralph/us-02-issue-customer-card",
  "description": "US-02 Issue a Customer Card (Digital) — CardNumber value object, the single issueCard path, and the digital card view. Exactly one valid card per customer; no printing (FD uses a separate system).",
  "userStories": [
    {
      "id": "US-001",
      "title": "CardNumber value object",
      "description": "As a developer, I need a value object for card numbers, so 50k3 is never handled as an ad-hoc string. Source: tasks/prd-us-02-issue-customer-card.md §US-02.1",
      "acceptanceCriteria": [
        "src/domain/card/cardNumber.ts exports format(customerNumber, index), parse(text) and next(cardNumber)",
        "format(50, 3) returns '50k3'; parse('50k3') returns {customerNumber: 50, index: 3}; next({50,3}) returns {50,4}",
        "parse returns a typed InvalidCardNumber error for: '', '50', 'k3', '50k0', '50k', '050k3', '50k3x' and negative numbers",
        "Input parsing accepts an uppercase K; output is always lowercase — documented in a comment",
        "Index starts at 1, never 0",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": "Exports formatCardNumber / parseCardNumber / nextCardNumber rather than bare format/parse/next — the module-suffixed names match the existing formatCardNumber and the parseGroup / parseWeekColour convention. parse throws InvalidCardNumber (added to errors.ts) rather than returning it, matching every other parser in the domain. Also rejects a padded index ('50k03') and customer number 0, and trims surrounding whitespace."
    },
    {
      "id": "US-002",
      "title": "issueCard use case — the single card-issuing path",
      "description": "As a developer, I need one use case that issues the next card and invalidates the previous one, reused by first issue, loss and stale-count reissue. Source: tasks/prd-us-02-issue-customer-card.md §US-02.2",
      "acceptanceCriteria": [
        "issueCard(deps, {customerId, reason}) creates a card with index = max existing index + 1, or 1 if none",
        "Validity is derived from being the highest index — no separate mutable valid flag exists",
        "Rejects with a typed error if the customer is archived",
        "Writes an audit entry with reason FIRST_ISSUE | LOST | STALE_COUNTS | OTHER",
        "Tested with fakes: first issue, second issue, archived customer, issue after a gap in indices (must take max+1)",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Lives in src/application/customers/issue-card.ts against a new CardRepository port (currentCard / issue). The port takes issue(customerId, {index, issuedAt, reason}) rather than the PRD's issue(customerId, index, reason): issuedAt must come from the injected clock, so infrastructure cannot be the one to stamp it. IssuedCard and CardIssueReason are declared in the new type-only src/domain/card/card.ts. Archived customers are refused with the new CustomerArchived error; blocked ones are served, since a block does not unregister. The registration card is still written inside customers.create — one transaction (US-01.4) — and gains its stored reason with the column in US-003."
    },
    {
      "id": "US-003",
      "title": "Card repository with currentCard query",
      "description": "As a developer, I need card persistence that guarantees exactly one valid card per customer. Source: tasks/prd-us-02-issue-customer-card.md §US-02.3",
      "acceptanceCriteria": [
        "Card model gains a reason field; @@unique([customerId, index]) is confirmed present",
        "PrismaCardRepository exposes currentCard(customerId) returning the highest index, and issue(customerId, index, reason)",
        "Integration test proves two different customers may both hold card number 50k1",
        "Integration test proves a concurrent double-issue cannot create two cards with the same index — the unique constraint surfaces as a typed error",
        "Migration committed under prisma/migrations/",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": true,
      "notes": "PrismaCardRepository lives in src/infrastructure/prisma/card-repository.ts and implements the port as declared in US-002 — issue(customerId, card) taking the whole IssuedCard, not (customerId, index, reason), so issuedAt stays the application's to stamp. The lost race surfaces as the new CardIndexTaken domain error. Adding the reason column unified NewCustomer.card with IssuedCard (CustomerCard is gone) and needed parseCardIssueReason, since SQLite has no enum type. The init migration was regenerated rather than amended, per CLAUDE.md's disposable-history rule; the hand-written partial unique index was re-added to its tail. PrismaCardRepository is wired into src/app/kunden/deps.ts so US-004 can reach it."
    },
    {
      "id": "US-004",
      "title": "Card view at /kunden/[id]/karte",
      "description": "As a staff member, I want to see all card information on screen, so I can transcribe it onto the physical card or feed it into the printing system. Source: tasks/prd-us-02-issue-customer-card.md §US-02.4",
      "acceptanceCriteria": [
        "Route /kunden/[id]/karte renders card number, first name, last name, group as a German coloured label, grown-up count and children count",
        "Counts are read live from the derived composition, never from a stored column",
        "The view states in German that this is the current card and which numbers it replaced",
        "Layout is card-shaped and legible across a desk; no print stylesheet and no PDF is produced",
        "All user-facing strings come from src/i18n/de.ts",
        "Typecheck passes",
        "Verify in browser using dev-browser skill"
      ],
      "priority": 4,
      "passes": true,
      "notes": "The route reads a new `readCard` use case (src/application/customers/read-card.ts) rather than widening `readCustomer` — the card asks a different question (the run of cards) than the overview does, and one view object serving both would carry fields each screen ignores. It needed a third card-port method, `listCards(customerId)`, returning the whole run highest index first; the head is the current card, so which card is valid is still read off the run and never off a flag. `/kunden/[id]` stays the customer overview and links on to the card. The PRD's 'reissue card' action is present but disabled, since its behaviour is US-09. Also extracted the twice-copied `germanDate` into src/i18n/format.ts. Browser-verified by rendering the route against seeded data (no MCP browser tool is configured in this sandbox); the visual layout is worth a human glance."
    },
    {
      "id": "US-005",
      "title": "E2E: card is issued on registration and displayed",
      "description": "As a developer, I need a Playwright spec proving registration produces a displayable k1 card. Source: tasks/prd-us-02-issue-customer-card.md §US-02.5",
      "acceptanceCriteria": [
        "Spec registers a customer, lands on the card view and asserts the card number matches ^[0-9]+k1$",
        "Spec asserts name, group and counts on the card match the registration input",
        "npm run test:e2e passes",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    }
  ]
}
